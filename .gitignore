# Claude Code 个人本地配置（不随仓库提交，见 docs/development-spec.md 附录 C）
.claude/settings.local.json

# OS
Thumbs.db
Desktop.ini
.DS_Store

# IDE
.idea/
*.swp

# 以下条目为「Use this template」直接建新项目时兜底（规范包自身无这些产物）
node_modules/
dist/
.env
.env.local
.env.*.local

# 后端构建产物
bin/
obj/

# E2E（Playwright）运行产物：失败截图 / trace / 报告，均不入库
# 注：tests/e2e/logs/ 是交付证据（被 docs/51、docs/52 逐条引用），刻意入库，不在此列
tests/e2e/.artifacts/
test-results/
playwright-report/

# E2E 部署产物：dotnet publish 输出（可重建），不入库
# 该目录下的 appsettings.Testing.json 会把 testing 签名密钥复制多份，
# 且每次重建内容都变、持续制造 diff 噪声
tests/e2e/deploy/

# 临时取证目录（`docs/role-protocol.md` §6：唯一前缀 + 登记绝对路径为证据）
# 注：与 tests/e2e/logs/ 同一口径——**证据文本入库，可重建产物不入库**。
# 这两类目录里 99% 的体积是 `dotnet build/test` 的输出（dll / pdb / exe / 应用宿主，
# 实测两个目录合计 104MB），入库会永久膨胀仓库，而它们可由源码重建。
# 刻意只放行**顶层 `.log`**：运行日志是「集成测试为何失败」的唯一原始证据，
# 被 docs/51、docs/52、docs/60 引用；目录内的 dll / pdb / exe / *.deps.json 一律不跟踪。
.tmp-*/**
!.tmp-*/*.log
# 注：放行的 `.log` 仅指**本仓运行日志**。dotnet 每次调用都会在 CWD 落一份
# `Microsoft.NET.Workload_<pid>_<日期>_<时间>_<毫秒>.log`（约 1KB 工具链遥测：实测 12:37:18 /
# 12:37:38 / 13:00:25 三次调用各生成一份，文件名含 PID 与时间戳 → 名字与内容每次调用都变）。
# 它既不是运行证据、又会持续制造新的未跟踪文件 → 在下一条放行之后**重新排除**
# （git 对同一路径按「末条匹配优先」判定，故本行必须在 `!.tmp-*/*.log` **之后**，不可上移）。
.tmp-*/Microsoft.NET.Workload_*.log
# 注：不做 `.tmp-*/**/*.log` 的放行——父目录一旦被上面的规则排除，git 不会再向下递归，
# 那条放行是死规则（看着像意图，实际永不生效）。需要嵌套日志时改用显式路径逐个放行。
