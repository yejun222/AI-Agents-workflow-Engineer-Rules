# 缺陷闭环状态机（权威判定表）

`feature.md` 第 7 步与 `test-executor` 的「建议结论」都引用本表。**结论字符串只在本文件定义一次**，任何分支引用的字符串必须与本表逐字一致（多一个字、少一个括号都会让字面匹配落空）。

## 输入事实

- **F1**：`docs/51-defects.md` 是否存在 `状态=未闭环` 的**致命 / 严重**缺陷？（**级别按下文 D3 判定**，不得凭语感给出）
- **F2**：`docs/52-qa-report.md` 的**事实部分**（缺陷分布、覆盖情况、证据路径）（**覆盖是否达标按下文 D1 判定**，不得自行解释）
- **F3**：主对话是否已在调度指令中**转达**「用户已接受」决定（含接受人、理由、补丁计划）？
- **F4**：主对话是否已明确指示「**带残留接受**」？
- **F5**：主对话是否已明确转达「**用户已接受建议级残留缺陷**」（含接受人、理由、补丁计划）？
- **F6**：主对话是否已明确转达「**用户已接受覆盖缺口**」（含接受人、理由、补丁计划）？

## 判据定义（判定顺序之前必读）

下表条件里的「**覆盖达标**」「**其他阻断项**」「**缺陷级别**」是本状态机的**输入**。三者在此**单点定义、闭集枚举**，任何角色不得各自解释——本表此前只在条件里引用它们、从未定义，结果是**同一份事实可以落到不同结论行**（如「98/98 已执行」这一句既被读作达标也被读作未达标、缺陷定级只能现场写散文论证）。

### D1　覆盖达标 / 覆盖不达标

**覆盖达标 = 下列三条同时成立**：

1. **需求覆盖完整**：`docs/10-prd.md` 的每条 AC 至少被 `docs/50-testcases.md` 的一条 TC 关联（关联关系由 test-designer 在 50 中显式声明，主对话抽查）。
2. **执行覆盖闭合**：分母 = 50 **当前版本的全部 TC 编号**（含标注「手动」者）；分子 = 本轮**已执行且给出明确结论**（通过 / 失败）的 TC。存在未执行 / 无法执行 / 结论不明确者 → **不达标**（手动用例同样计入分母，必须有执行记录：执行人、时间、结论）。
3. **无静默缩表**：任何使分母变小的动作（删用例、合并用例、降级为「不作要求」）必须经契约裁决并留下**编号级**记录；禁止以「这条没法测」为由把 TC 移出分母——否则分母可被改小来凑达标。

- **「不可执行」不豁免登记**：无法执行的 TC 一律按 `docs/artifacts.md` §3.1 登记为 OBS（含责任人 + 计划执行时点），**其存在本身即判覆盖不达标**，除非经 `F6` 显式接受（→ 行 3.5）。
- **覆盖达标 ≠ 质量达标**：前者只回答「该测的是否都测了」；通过与否由缺陷台账与级别决定。两者不可互相替代、不可互相放行。

### D2　其他阻断项（行 4 用）

除「未闭环致命 / 严重」（由行 0 单独拦截）与「覆盖不达标」外，下列**被显式登记**的项构成阻断：

1. 未处置的「**契约冲突上报**」或未消解的「**待确认清单**」项；
2. 被**显式标注为「阻断」的 OBS**（环境 / 证据 / 工具链类）——`52:OBS-nn` 由 test-executor 标注、`60:OBS-nn` 由 code-reviewer 标注，**逐条标注「阻断 / 非阻断」**。**标注必须是可机械提取的结构化字段，不得只写「不再构成任何阻断」「不作为阻断项」这类散文式否定**——否则本项无法判定，本行整条落空（实测：52 的 OBS-10 / OBS-11 即是此形）；**漏标注者按「阻断」处理**（推定阻断，逼登记方表态）；
3. 上游契约为「**待复核**」状态、失效传播矩阵要求的复核未完成（`docs/artifacts.md` §5：不重跑不能进入下一阶段）；
4. `docs/60-review.md` 中状态为 `未闭环` 的 `REV-xx` 审查问题（致命 / 严重级已由 `/feature` 第 5 步单独拦截并给出接受路径，此处不再重复；**一般 / 建议级 REV 此前不在任何判定行的条件里**，修复循环 a 却把「60 未闭环审查问题」列为 engineer 的派活触发条件 —— 那是一个没有任何判定行能产生的进入条件，现由本项补齐）。

### D3　缺陷级别（F1 与各行判定用）

级别由**基准档 + 降级规则**唯一确定，不由各角色凭语感给出：

| 基准档 | 判据（按 ① 判定） | 本仓定级实例（校准用，均为已签署产物中的历史定级） |
| --- | --- | --- |
| **致命** | 业务终态被破坏且不可恢复（数据 / 资金 / 库存 / 权限终态错误） | （本仓无实例） |
| **严重** | 业务终态错误或产生多余副作用；或安全边界可被绕过 | （本仓无实例） |
| **一般** | **业务终态正确**（冲突被识别、事务整体回滚、可自行恢复），但有**功能偏差**：机制层面失效、对外语义偏差（错误码分层、契约状态码）、用户可见行为错误 | BUG-01（HTTP 码与契约不符，未产生第二次副作用）、BUG-05（重试链对后半段写入点失效，但整体回滚、可自行重试恢复） |
| **建议** | 业务终态正确且不变量全满足，且**无功能偏差**：可维护性 / 一致性 / 文案 / 序列化格式 / 门禁类问题 | BUG-02（时区标识）、BUG-03 与 REV-21（文案与冻结面不一致）、REV-20（格式门禁） |

- **降级只允许一档，且限于展示层**：仅当偏差**限于展示层**（文案 / 名称 / 格式）、**触发窗口极小**（竞态 / 特定时序）、且业务终态与不变量全满足时，可降一档——BUG-04（竞态下弹层奖品名为空、中奖记录本身正确）即按此由「一般」降为「建议」。
- **不得降级的两类**：① 触及**数据完整性与安全边界**的，直接按致命判；② **机制层面失效 / 对外语义偏差**（重试链失效、错误码分层被绕过、契约状态码不符）不适用降级——它们说明的是结构性缺陷，不是"偶尔才遇到"。
- **每条缺陷 / 审查问题必须写明定级依据**：对应上述判据的**实测事实**（终态如何、数据是否受损、触发条件、影响面），不得只写级别。
- 对定级有异议走 `docs/51-defects.md` §6「定级异议登记」，由主对话裁决。
- **本表用于今后的定级**；已签署产物中的历史定级不回改（见 `docs/development-spec.md` 13.4：历史记录不回改）。

## 判定顺序（从上到下，首个命中即结论）

| # | 事实条件 | 结论 | 主对话动作 |
| --- | --- | --- | --- |
| 0 | **F1 = 是** 且 **F4 = 否**（安全网：无论 F2 结论为何；F4 判断依据 = 主对话是否已明确指示「带残留接受」） | **不可发布** | 进入修复循环（防"质评结论与缺陷台账不一致"） |
| **0.5** | 无未闭环致命 / 严重；**覆盖达标**；**无其他阻断项（D2）**；存在**未闭环建议级**缺陷且 **F5 = 未转达** | **不可发布（待确认接受）** | 暂停，把建议级缺陷清单交用户：选「**接受**」→ 转达接受决定（含接受人 / 理由 / 补丁计划）后**重新调度 test-executor，把 52 落为「可发布」**（该缺陷状态改为 `已接受`，并**逐条列入交付总结的已接受残留清单**），再按行 1 判 `可发布`；选「**返工**」→ 进入修复循环。**两种情况都不要自行进入修复循环** |
| 1 | 无未闭环致命 / 严重；覆盖达标、**无其他阻断项（D2）**、无一般缺陷 | **可发布** | 进入交付 |
| 2 | 无未闭环致命 / 严重；**覆盖达标**；**无其他阻断项（D2）**；存在一般缺陷且 **F3 = 已转达接受** | **有条件发布** | 重新调度 test-executor（输入 51/52 原文 + 转达接受决定），把 52 落为「有条件发布」（接受人 / 理由 / 补丁计划），对应缺陷状态改为 `已接受`；交付。**不进修复循环** |
| 3 | 无未闭环致命 / 严重；**无其他阻断项（D2）**；存在一般缺陷且 **F3 = 未转达** | **不可发布（待确认接受）** | 暂停，把一般缺陷清单交用户：选择「接受」→ 转达接受决定后**按本表重判**（覆盖达标且无其他阻断项 → 行 2 命中，按行 2 落盘 `有条件发布`；**覆盖不达标 → 重判落到行 4，不得据此把覆盖缺口一并吸收进 `有条件发布`**）；「返工」→ 进入修复循环。**两种情况都不要自行进入修复循环** |
| **3.5** | **无未闭环致命 / 严重**；**覆盖不达标**；**无其他阻断项（D2）**；且 **F6 = 已转达**（主对话已明确转达「用户已接受覆盖缺口」） | **有条件发布** | 重新调度 test-executor，把 52 落为「有条件发布」（**覆盖缺口编号 / 接受人 / 理由 / 补丁计划**；若同时存在**已接受的一般缺陷**，一并登记其接受人 / 理由 / 补丁计划 —— 该缺陷经由行 3 接受、但因覆盖不达标使行 2 不命中，除本处外无落盘位置）；对应缺口逐条列入交付总结的**已接受残留清单**；交付。**不进修复循环** |
| 4 | 无未闭环致命 / 严重；但覆盖不达标或存在其他阻断项 | **不可发布** | 进入修复循环 |
| 5 | **F1 = 是** 且 **F4 = 已指示「带残留接受」** | **有条件发布（含残留缺陷）** | 重新调度 test-executor，把 52 落为「有条件发布（含残留缺陷）」（缺陷编号 / 接受人 / 理由 / 补丁计划；**报告首段必须显式标注与「致命 / 严重未闭环即不可发布」硬规则的冲突与接受人**）；对应缺陷状态改为 `已接受`；**本行刻意不含 D2 前提（理由见「判定顺序」节表注）—— 抵达本行时其他阻断项**（D2 四类：未处置的契约冲突 / 未消解的待确认清单项、被显式标注为「阻断」的 `52:OBS-nn` / `60:OBS-nn`、上游「待复核」、未闭环 `REV-xx`）**一并被接受，但必须逐条登记披露**：逐条记入 52 与交付总结的**已接受残留清单**（各含接受人 / 理由 / 补丁计划）—— **本行是它们在本表中唯一的落盘位置**（与行 3.5 登记「经行 3 接受的一般缺陷」同构）；交付 |

> **表注（修订本表前必读）—— 下列是「现行约束」，不是历史记录**：它们解释本表**为什么是现在这个形状**，改表前先读。纯粹的历史因果（每次修订当时为何这么改）已移入文末「修订记录（历史，不回改）」节。
>
> - **行序即语义**（本表规则是「从上到下，首个命中即结论」—— 行的**相对位置本身就是判定条件**）：
>   - **行 0.5 必须位于行 1 之前**：行 1 的条件「无一般缺陷」**不**被建议级缺陷否定，一旦下移，行 1 会先命中判 `可发布`，建议级未闭环缺陷仍被静默吸收，而行 0.5 **永远不可达**。
>   - **行 3.5 必须位于行 4 之前**：行 4 的条件「覆盖不达标或存在其他阻断项」是行 3.5 的**真超集**，一旦下移，行 3.5 **永远不可达**。
>   - **行 3.5 必须带「无未闭环致命 / 严重」前提**：省略它，`F1 = 是` + 覆盖不达标 + `F6 = 已转达` + `F4 = 已指示` 的场景会被行 3.5**抢先截获为 `有条件发布`**，使行 5 的 `有条件发布（含残留缺陷）` 沦为**死分支**、残留的致命 / 严重缺陷被**降级静默吸收**。
>   - **行 3.5 刻意位于行 3 之后**：若同时存在「未转达接受的一般缺陷」，须先由行 3 暂停交用户决断（一般缺陷的接受决定**优先于**覆盖缺口），不被行 3.5 一并放行。
>   - **覆盖前提不得少写**（行 0.5 / 行 2 带「覆盖达标」、行 3.5 带「覆盖不达标」）：覆盖不达标时行 0.5 / 行 2 不命中，继续下落到行 3.5（`F6 = 已转达` → `有条件发布`）或行 4（`F6 = 未转达` → `不可发布`，走修复循环）；**任何一行都不得把覆盖缺口静默吸收** —— 缺口须由 test-designer / test-executor 补做处置，或经用户**显式接受**（`F6`）。
> - **行号不得重排**：行 0 / 行 5 被 `feature.md` 第 7 步引用（原文「含行 0 的 F4 前提、行 5 的残留接受路径」）；行 5 / 行 3.5 被本文件「修复循环」节引用；行 0 / 0.5 / 1 / 2 / 3 / 4 被 `docs/52-qa-report.md` §2 的逐行命中表引用（该表为 R7 后重写版，未含行 5 与行 3.5）。重排会使这些引用**全部静默失效**。新增行一律取小数位（`0.5` / `3.5`），其数值表示判定位置在相邻两行**之间**。（**2026-09-18 勘误**：原句称「行 1 / 2 / 3 / 4 / 5 已被 `feature.md` 第 7 步、本文件「修复循环」节、`docs/51-defects.md` 与 `docs/52-qa-report.md` 的逐行命中表引用」——逐处实测不成立：`docs/51-defects.md` **一处判定行引用都没有**，`feature.md` 第 7 步只引用行 0 与行 5，`52` 的命中表为行 0 / 0.5 / 1 / 2 / 3 / 4。）
> - **行 0.5 / 1 / 2 / 3 / 3.5 各自都带「无其他阻断项（D2）」是刻意的，不是冗余**：D2 自身标注的用途是「行 4 用」，而这五行都排在行 4 **之前** —— 少任何一处，D2 项在**覆盖达标**时就没有任何判定行能拦住（D2 恒为死条件），只能被静默吸收。同一失效的修法是**补前提**、而非**新增行**：新增行要占小数位并改变「首个命中」的相对顺序；该改法与 2026-09-17 为行 2 补「覆盖达标」**同构**。
> - **行 5 是本表唯一的「D2 刻意例外」**（它的条件里**不含**该项）：`F1 = 是` 时行 0.5–4 因「无未闭环致命 / 严重」全部不命中，若再给行 5 加该前提，则 `F1 = 是` + `F4 = 已指示` + 存在 D2 项的场景**无任何行命中**（本表在行 5 之后**无兜底行**）。故 D2 项在行 5 **不是被放行，而是被一并接受并逐条披露**：行 5 的落盘动作要求逐条登记 D2 四类，**行 5 是它们在本表中唯一的落盘位置**（与行 3.5 登记「经行 3 接受的一般缺陷」同构）。⚠️ 第四次修订曾把该情形表述为「使 D2 项**一律**下落到行 4」—— **实测该表述在 `F1 = 是` 时不成立**，已订正（见文末修订记录第五次）。
> - **刻意副作用**：行 3 补 D2 前提后，「一般缺陷未转达 + 存在阻断项」不再暂停交用户，而是**先落行 4 走修复循环清障**；一般缺陷的接受决定仍会在修复循环出口 **c** 的残留项清单中被提出，顺序由「先表决、再清障」变为**先清障、再表决**。
>
> **守护**：行 3.5 的位置与前提由 `tools/check-config.py` 的 **T11** 断言守护；D2 四类与五行的 D2 前提由 **T24** 守护；**本表注**（行序约束 / 行 5 的 D2 例外与披露义务 / 与历史节的分离）由 **T29** 守护（三条断言的锚句**均已纳入 `--self-test` 突变**）。改动任一处的措辞都会使断言 FAIL —— 这是**预期行为**，同步改另一处即可，**不要删断言**。

## 修复循环（a → b → c）

- **a** 按触发原因分派（只派 engineer 修不了覆盖缺口）：
  - 缺陷类（51 未闭环缺陷 / 60 未闭环审查问题）→ 调度 engineer：输入 `docs/51-defects.md`（或 `docs/60-review.md` 问题清单）+ `docs/50-testcases.md`（关联 TC 复现步骤）+ 相关上游契约 + `docs/40-changelog.md` → 修复并追加 CHG 记录；
  - 覆盖不达标 / 用例缺口类 → 调度 test-designer 补 `docs/50-testcases.md` 用例（或 test-executor 补执行并回写 50 结果）。
  - **其他阻断项（D2 四类）—— 此前无 owner，行 4 到达 a 会原地停滞，必须逐类分派**：
    - ① 契约冲突上报 / 待确认清单 → **主对话裁决**（子代理无权修改上游契约，见 `docs/role-protocol.md` §4），裁决后按 `docs/artifacts.md` §5 失效传播矩阵重跑受影响的下游角色；
    - ② 阻断型 OBS（`52:OBS-nn` / `60:OBS-nn`）→ **按领域归属**：环境 / 证据 / 工具链类由**主对话调度**（起环境、补依赖、补持久证据）；实现类 → engineer；用例类 → 测试角色；
    - ③ 上游「待复核」→ 按 `docs/artifacts.md` §5 失效传播矩阵重跑对应角色，并在其产物头部清除失效标记；
    - ④ 未闭环 `REV-xx`（含被标注为阻断的 `60:OBS-nn`）→ **code-reviewer 复核**；若复核结论要求改代码，再派 engineer，并由 code-reviewer 重跑更新状态。
- **b** 重新调度 test-executor 回归：输入 `docs/51-defects.md` 待回归缺陷与当前状态 + `docs/40-changelog.md` 本次修复改动 + 相关上游契约 → 更新 `docs/50-testcases.md`（回写结果）、`docs/51-defects.md`（逐条更新状态）与 `docs/52-qa-report.md`。
- **c** 自动循环最多 2 轮；**出口与缺陷级别解耦**：第 2 轮后只要存在任何未消除的阻断项（未闭环致命 / 严重缺陷、覆盖不达标**且未经 `F6` 接受**、其他阻断项、尚未经用户接受的一般缺陷、尚未经用户接受的建议级缺陷），**一律停止并把残留项清单交用户决断**——不只认致命 / 严重，否则行 3 返工、行 4 覆盖缺口这类不含致命 / 严重的场景会在「重判 → 再进循环」之间静默空转。**按用户选择执行**（每次人工续跑后回到本决断点，直到用户明确选择「带残留接受」/「接受覆盖缺口」或「终止」为止——不会因行 0 自动无限循环）：
  - 「**继续修复**」→ 再跑一轮 a + b（此轮为人工触发，**不计入自动循环上限（最多 2 轮）**——该上限只约束由缺陷状态自动触发的循环；人工触发的重跑不受此限）；
  - 「**带残留接受**」→ 转达 `F4` 后**按本表重判**（`F1 = 是` 且 `F4 = 已指示` → 行 5 命中；行 0.5–4 均带「无未闭环致命 / 严重」前提而不命中，故行 5 是**唯一落点**）。⚠️ **本出口只在 `F1 = 是`（存在未闭环致命 / 严重）时有意义**：`F1 = 否` 时没有可「带残留」的致命 / 严重缺陷，选它不会命中任何行；
  - 「**接受覆盖缺口**」→ 转达 `F6`（含接受人 / 理由 / 补丁计划）后**按本表重判**：覆盖不达标且**无其他阻断项（D2）** → 行 3.5 命中（`有条件发布`）；**存在 D2 项 → 行 4 命中（`不可发布`）**。⚠️ **D2 在本表中没有任何接受路径**（行 5 的 D2 例外只在 `F1 = 是` 时生效，见「判定顺序」节表注）——存在 D2 项时不能靠本出口清障，只能继续修复或终止。⚠️ 本出口**只对覆盖缺口有效**，不得用于绕过未闭环致命 / 严重缺陷——后者只有「带残留接受」一条路。
  - 「**终止**」→ 立即停止流程，输出当前全部产物路径与未闭环缺陷清单，不再调度任何子代理。

## 结论字符串（唯一枚举）

```
可发布 ｜ 有条件发布 ｜ 有条件发布（含残留缺陷） ｜ 不可发布（待确认接受） ｜ 不可发布
```

> **test-executor 的边界**：test-executor 只能基于 F1/F2 给出**建议结论**（对应行 0 / 行 0.5 / 行 1–3 / 行 4 范围）；**行 3.5 与「有条件发布（含残留缺陷）」均不在其建议范围内** —— 前者须 `F6`、后者须 `F4` 由主对话转达后，才由 test-executor 在**重调度**中落盘（行 3.5 落为「有条件发布」并登记覆盖缺口编号 / 接受人 / 理由 / 补丁计划）。最终结论一律由主对话按本表判定并在交付总结中签署。

## 修订记录（历史，不回改）

> 本表历次修订的**历史因果**（当时为何这么改），按 `docs/development-spec.md` **13.4** 保留、**不准回改**。**现行的形状约束不在本节**——行序要求、行 5 的 D2 例外与披露义务见「判定顺序」节下方的表注；已签署产物中的历史判定同样不回改。

- **2026-09-17（用户裁决）· 为行 2 补「覆盖达标」前提**：行 2 原条件为「无未闭环致命 / 严重；存在一般缺陷且 F3 = 已转达接受」，**不含覆盖条件**。因本表规则为「从上到下，首个命中即结论」，行 2 会先于行 4（覆盖不达标）命中，导致**用户接受一般缺陷后，未执行的用例被静默吸收进「有条件发布」而不出现在结论里**。故为行 2 补上「**覆盖达标**」前提：覆盖不达标时行 2 不再命中，落到行 4 → `不可发布` → 进入修复循环。结论字符串（唯一枚举）未变更。
- **2026-09-17（用户裁决 · 第二次）· 新增 `F5` 与行 0.5**：本表原对**建议级未闭环缺陷**无任何接受路径 —— **行 1** 的条件为「无**一般**缺陷」（建议级**低于**一般，不构成其否定条件），**行 5**「带残留接受」的前提为 `F1 = 是`（存在未闭环致命 / 严重），故建议级未闭环缺陷**在结论中静默消失、且无任何人对它表态**。现新增事实变量 **`F5`** 与判定行 **行 0.5**，给予其显式接受路径。行号取 `0.5` 表示其判定位置在**行 0 与行 1 之间**。
- **2026-09-17（用户裁决 · 第三次）· 新增 `F6` 与行 3.5**：本表原只有 `F4`「带残留接受」覆盖**致命 / 严重缺陷**（行 5），**没有覆盖缺口的接受路径** —— 用户明确表示「接受覆盖缺口、不补做」时，行 4 仍判 `不可发布` 并强制进入修复循环，与该意愿冲突。现新增事实变量 **`F6`** 与判定行 **行 3.5**，给予其显式接受路径（结论复用既有枚举成员 `有条件发布`，**未新增结论字符串**）。变量名取 `F6` 而非 `F5`：`F5` 已被「建议级残留缺陷」占用，沿用会造成两处撞名。
- **2026-09-18（用户裁决 · 第四次）· 为行 0.5 / 1 / 2 / 3 / 3.5 补「无其他阻断项（D2）」前提**：本表原只在**行 4** 的条件里含该项，而**行 0.5 / 1 / 2 三条都在行 4 之前、且都不含该项**。后果：处于「无未闭环致命 / 严重 + 覆盖达标」时，**D2 的四类阻断项没有任何判定行能拦住**——无一般缺陷走行 1（`可发布`）、有建议级缺陷走行 0.5、一般缺陷已转达走行 2，三条都把 D2 项**静默吸收**；「或存在其他阻断项」这半句只有在**同时覆盖不达标**时才可能经行 4 生效。即：D2 自身标注的用途「行 4 用」，在覆盖达标时**恒为死条件**，六条行里三条是它的旁路。**行 4 的条件未改**（它仍是 D2 的终局），故行号与既有引用零影响。
  - **同批连带修正**：① 行 0.5 的接受分支原**无落盘动作**（行 2 / 行 3.5 均有「重新调度 test-executor 落盘」），且其结论为 `可发布` 而非 `有条件发布`，套不上 test-executor 输出要求第 3 条的措辞 —— 结果是**被接受的建议级缺陷在 51 中永远停在 `未闭环`、52 却给出 `可发布`**，二者矛盾无人负责；现补「52 落为 `可发布` + 该缺陷状态改 `已接受`」。② 行 3 的接受分支原写死「按行 2 执行」，而行 2 已带「覆盖达标」前提 —— **照字面执行会把覆盖缺口吸收进 `有条件发布`**，正是第二次修订要防的失效；现改为「按本表重判」并显式点明两种落点。③ D2 第 2 项范围原只限「test-executor 在 52 中标注」，使 `60:OBS-nn` 的**强制登记义务**（`docs/artifacts.md` §3.1）对任何结论的权重恒为 0 —— 现扩为「52 / 60 各自标注」，并新增第 4 项纳入未闭环 `REV-xx`（此前修复循环 a 把它当作 engineer 的派活触发条件，却没有任何判定行能产生该进入条件）。④ D2 第 2 项补「漏标注按阻断推定」：散文式否定（实测 52 的 OBS-10 / OBS-11）无法机械提取，缺失标注时推定阻断**才能逼登记方表态**。
- **2026-09-18（用户裁决 · 第五次）· 订正一处与行为不符的实然陈述，并给行 5 补披露义务**：第四次修订的表述称「使 D2 项**一律**下落到行 4」——实测该说法不成立：`F1 = 是` 且 `F4 = 已指示` 时，**行 0.5 / 1 / 2 / 3 / 3.5 与行 4 都带「无未闭环致命 / 严重」前提而不命中**，**D2 项由行 5 命中并静默吸收**，而行 5 原落盘动作只要求登记「缺陷编号」，D2 四类中除缺陷者外**均无落盘位置**（本仓「致命 / 严重」两类在 D3 表中标注「无实例」，故该分支从未被踩到）。处置：**给行 5 补「一并登记被一并接受的 D2 四类」的披露义务**（不改行序、不新增判定行），并把「行 5 是本表唯一的 D2 刻意例外」写进表注。
- **2026-09-18（用户裁决 · 第六次）· 订正修复循环出口 c 的写死行号、订正表注的行号引用清单、补齐 T11 突变**：三处同源于一次全仓审计。① 出口 c 的「带残留接受」/「接受覆盖缺口」原分别写死「按上表**行 5** / **行 3.5** 执行」——行 5 需 `F1 = 是`、行 3.5 需「无其他阻断项（D2）」，前提不成立时照字面执行会落到一个**没有任何行命中的空转**上（与第四次修订订正行 3「按行 2 执行」同型）；现改为「转达后按本表重判」并写明两种落点，同时显式声明 **D2 在本表中没有接受路径**（行 5 的例外只在 `F1 = 是` 时生效），避免把「接受覆盖缺口」当成清 D2 障的入口。② 表注「行号不得重排」一句原称行 1 / 2 / 3 / 4 / 5 被 `feature.md` 第 7 步、本文件「修复循环」节、`51` 与 `52` 的逐行命中表引用——逐处实测不成立（`51` 无任何行号引用、`feature` 只引行 0 与 5、`52` 的命中表不含行 5 与行 3.5），已按实测改写。③ 表注原称三段约束「均含突变」，实测 **T11 的 9 条断言零突变覆盖**（`--self-test` 的突变此前只到 T16 / T20 / T24 / T29 段），已为行 3.5 的位置与前提补 5 条突变（含两条位置突变、前提 / F6 / 结论枚举各一条）——断言本身不变，只补负向验证。
