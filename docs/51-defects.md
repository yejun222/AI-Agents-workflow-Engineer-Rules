# 缺陷报告：转盘抽奖（前后端分离）

| 项 | 内容 |
| --- | --- |
| 产物 | 缺陷报告（defects） |
| 版本 | v1 |
| 冻结时间 | 2026-09-17T18:31:00+08:00（**R7 回写的实测读数** `18:31:01` 向下取整到分钟——即该次落盘本身，按 `docs/artifacts.md` §4「冻结时间的判据」即「末次实际落盘时刻」）（**2026-09-18 勘误**：① 本条原附的推导**不成立**：原文所称「实际落盘时刻 `17:18:40`」向下取整应为 `17:18`，而取值 `18:31` 实为 R7 回写**同批同源**的读数（与 `docs/52-qa-report.md` 冻结时间行同值）—— 原句把两个不同来源的时刻混写成了同一次推导；**取值本身无误，只订正推导与证据来源**。② 另登记本条订正**之后**的一次落盘：「OBS 编号勘误」块自标日期 `2026-09-17`，而承载它的提交为 **`2026-09-18 03:21:59`**（`7c163f7`）—— 该块的**实际落盘日无法由证据确证**（可能写于 09-17 夜间、09-18 凌晨才提交；同一提交内含 09-18 02:46–03:02 产生的日志），按 §4「落盘证据只认提交时刻」只记其**落盘上界** = `2026-09-18 03:21:59`，**不推定**。该块属**引用勘误**（不涉契约内容），按 §4 第 5 条**不改版本**，故此处**只登记、不升版**）；④ 另登记本条订正**之后**的又一次落盘（2026-09-18）：新增「执行侧补验标签勘误」行（`TC-13b` / `TC-39b` 登记与订正）+ `TC-13` 行的引用订正（行号引用改为稳定 ID）。落盘时刻实测 = `2026-09-18T11:33`（`date` 实测输出，向下取整到分钟）；**承载提交尚未产生**（本角色不提交代码，须由主对话 wip 提交承载）→ 按 §4 只登记落盘日 `2026-09-18`、**不推定提交 hash**。**（2026-09-20 承载提交回填）**：承载提交 = `6cc4c10`（全量 `6cc4c10e97c946fbfe5a148c82d0bd5c94c1385e`）、提交时间 `2026-09-18T17:57:11+08:00`（本角色 `git log -S` pickaxe 实测：该批落盘文本由 `6cc4c10` 引入、`60b6cf4` 未改动该串）；本回填自身由**下一次提交**承载。本批属**引用勘误**，按 §4 第 5 条**不改版本**。）；**⑤ 2026-09-20 落盘**：§3 观察项表新增 `51:OBS-12` 行 + 头部新增两行（台账回写 / 新增观察项登记 + `60-review` 台账回写轮同步与 `60:OBS-32` 扇出复核）。落盘时刻实测 = `2026-09-20T11:00:32`（python datetime.now()，本机 `date` 不可用）；**承载提交尚未产生**（本角色不提交代码）→ 按 §4 只登记落盘日 `2026-09-20`、**不推定提交 hash**。**（2026-09-20 承载提交回填）**：承载提交 = `60b6cf4`（全量 `60b6cf411a3fef20501cdfd145ea3ffb4b3bf31f`）、提交时间 `2026-09-20T12:56:37+08:00`（主对话 `git log` 实测）；本回填自身由**下一次提交**承载。本批属**台账类回写**（新增观察项登记 + 状态同步复核），按 §4 第 5 条**不升版**。**⑥ 2026-09-20 Wave D 落盘**：`51:OBS-12` 闭环回写（状态置 `已闭环`、末列「阻断」重标 `非阻断`）+ 新增 `51:OBS-13` / `51:OBS-14` 两行 + 头部新增两行（闭环 / 新增观察项登记行，`tests/integration` 夹具变更的 §5 扇出复核行）。落盘时刻实测 = `2026-09-20T11:44:49`（python datetime.now()，本机 `date` 不可用）；**承载提交尚未产生**（本角色不提交代码）→ 按 §4 只登记落盘日 `2026-09-20`、**不推定提交 hash**。**（2026-09-20 承载提交回填）**：承载提交 = `60b6cf4`（全量 `60b6cf411a3fef20501cdfd145ea3ffb4b3bf31f`）、提交时间 `2026-09-20T12:56:37+08:00`（主对话 `git log` 实测）；本回填自身由**下一次提交**承载。本批属**台账类回写（状态回写 / 阻断标注 / 新增观察项登记）+ 复核结论登记**（任何 BUG 状态与判定结论均未变）→ 按 §4 第 5 条**不升版**。**（2026-09-20 取值订正）**：⑥ 原记落盘时刻 `2026-09-20T11:43:49` 系批内较早一次读数、比该批末次实际写入早 1 分钟；按 `docs/artifacts.md` §4「取值 = 该版本末次实际落盘时刻」**订正为 `2026-09-20T11:44:49`**（证据链 = **本角色自报 + 主对话复核文件 mtime**；注意：按 §4「落盘证据只认 git 提交时刻」，**文件 mtime 不作落盘证据** —— 本项仅为取值订正、不主张落盘证据强度；**承载提交 hash + 提交时间 = `60b6cf4`（全量 `60b6cf411a3fef20501cdfd145ea3ffb4b3bf31f`）/ `2026-09-20T12:56:37+08:00`（2026-09-20 承载提交回填；主对话 `git log` 实测）**，提交后由主对话调度本角色回填；本回填自身由**下一次提交**承载）。**⑦ 2026-09-20 B 批落盘**：§3 观察项表 6 条阻断型行（`51:OBS-03` / `51:OBS-05` / `51:OBS-06` / `51:OBS-07` / `51:OBS-08` / `51:OBS-11`）描述列末尾补对侧编号标注 + 头部新增一行（本批台账回写 / 取值订正登记行）。落盘时刻实测 = `2026-09-20T12:03:14`（python datetime.now()，本机 `date` 不可用）；**承载提交尚未产生**（本角色不提交代码）→ 按 §4 只登记落盘日 `2026-09-20`、**不推定提交 hash**。**（2026-09-20 承载提交回填）**：承载提交 = `60b6cf4`（全量 `60b6cf411a3fef20501cdfd145ea3ffb4b3bf31f`）、提交时间 `2026-09-20T12:56:37+08:00`（主对话 `git log` 实测）；本回填自身由**下一次提交**承载。本批属**台账类回写（阻断型 OBS 行对侧编号标注）+ 头部元信息取值订正**（不改任何阻断列取值、不改任何判定结论）→ 按 §4 第 5 条**不升版**。**⑧ 2026-09-20 三态批次落盘**：§3 观察项表 4 行末列改判为 `已接受（用户决断）`（`51:OBS-03` / `51:OBS-06` / `51:OBS-07` / `51:OBS-11`，逐行含接受人 / 理由 / 补丁计划三要素）+ `51:OBS-08` 行追加修复进展登记（取值维持「阻断」、闭环待终批独立复现）+ 待确认清单 `QX-E-01` / `QX-E-02` 状态更新 + 头部新增一行（本批登记）。落盘时刻实测 = `2026-09-20T14:07`（**实测**：工具 = `date`（Git Bash），原始输出 `2026-09-20 14:07:19 +0800`，向下取整到分钟）；**承载提交尚未产生**（本角色不提交代码，须由主对话 wip 提交承载）→ 按 §4 只登记落盘日 `2026-09-20`、**不推定提交 hash**。本批性质 = 台账类回写（OBS 状态改判 / 阻断标注 / 待确认项状态更新 / 修复进展登记）→ 按 §4 第 5 条**不升版**（BUG-01…05 均维持 `已闭环`、未闭环缺陷 0 条、§4 未执行用例 0 条均不变）。**⑨ 2026-09-20 终批前半段落盘**：§3 观察项表 `51:OBS-05` 闭环回写（状态 → `已闭环`、末列「阻断」→「非阻断」）+ `51:OBS-08` 修复进展登记（取值维持「阻断」，闭环待程序载体落地）+ §3 表下问答块追加「终批前半段同步」段 + 本头部新增本批登记行。落盘时刻实测 = `2026-09-20T14:48`（**实测**：工具 = `date`（Git Bash），原始输出 `2026-09-20 14:48:27 +0800`，向下取整到分钟）；**承载提交尚未产生**（本角色不提交代码，须由主对话 wip 提交承载）→ 按 §4 只登记落盘日 `2026-09-20`、**不推定提交 hash**。本批性质 = 台账类回写（OBS 状态回写 / 进展登记）→ 按 §4 第 5 条**不升版**（BUG-01…05 均维持 `已闭环`、未闭环缺陷 0 条、§4 未执行用例 0 条（98/98）均不变）。 **⑩ 2026-09-20 终批后半段落盘**：§3 观察项表 `51:OBS-08` 闭环回写（状态 → `已闭环`、末列「阻断」→「非阻断」）+ 新增 `51:OBS-15`（TC-81 判据面升级未复跑 + 2 条候选扫描面缺位；非阻断）+ §3 表下问答块追加「终批后半段同步」段 + §1 最高严重程度行追加 `60` v8 追记+ 头部新增三行（`40-changelog` CHG-21 失效标记 / 复核结论行、`60` v7 → v8 失效标记 / 复核结论行、本批台账回写登记行）。落盘时刻实测 = 2026-09-20 15:59:10 +0800（**实测**：工具 = `date`（Git Bash），原始输出 2026-09-20 15:59:10 +0800）。**承载提交尚未产生**（本角色不提交代码，须由主对话 wip 提交承载）→ 按 §4 只登记落盘日 `2026-09-20`、**不推定提交 hash**。本批性质 = 台账类回写（OBS 状态回写 / 阻断标注 / 新增观察项 + 失效标记 / 复核结论登记）→ 按 §4 第三类**不升版**（BUG-01…05 均维持 `已闭环`、未闭环缺陷 0 条、§4 未执行用例 0 条（98/98）均不变）。 |
| 上游依赖 | `docs/50-testcases.md` **v3**（98 条）/ `docs/10-prd.md` **v2** / `docs/30-architecture.md` **v4** / `docs/error-codes.md` / `docs/60-review.md` **v6**（**2026-09-18 勘误**：原值 50 **v2** / 30 **v3** / 60 **v4** 与本文件**下方失效标记行**所记录的上游版本不符——那几行已分别记录 50 → v3、30 v3 → v4（R7 回写）、60 v5 → v6（CHG-18 同步行）；50 取 **v3** 是依本文件的失效传播记录（本文件从未记录消费 50 v4），与 `docs/52-qa-report.md` 行的 50 **v4** 不冲突——同批两份文件各自的事实） |
| 失效标记 / 复核结论（2026-09-17） | 上游 `docs/60-review.md` 更新至 **v3**（CHG-14 后重审），本产物基于 v2 复核 → **已复核，结论：无需变更**。60 v3 §B 经独立磁盘核验确认 CHG-14 的 **BUG-01 / BUG-02 / BUG-03 三项修复正确**，与本文件 §1 的 `已闭环` 判定**一致（互相印证，非冲突）**；§A 的 5 项未闭环 REV（REV-10/11/12/16/18）均为建议级且用户已裁决不纳入本轮 → 按纳入规则**不进入本台账**；本轮新增 **REV-20（建议级）**属 code-reviewer 的**格式门禁发现、非测试发现缺陷** → **不登记为 BUG**，已记入 `docs/52-qa-report.md` §3 **OBS-10**。据 60 v3 §D「低于门槛的残余观察」第 3 条：BUG-01「409 未列入 `error-codes.md`」的附带口径问题**已正式关闭**（设计内），见 §2 BUG-01 详情补注。 |
| 失效标记（`50-testcases` v3 触发回写，2026-09-17） | 上游 `docs/50-testcases.md` 变更至 **v3**（仅 TC-76 第三条款口径订正，依用户裁决「解释 1：判 TC-76 用词过严，改用例」）→ 按失效传播矩阵回写本台账：**§0 R4 行**（TC-76 由「3/4 子项通过」→ **4/4 全绿**）、**待确认清单 QX-E-07**（待裁决 → **已裁决并闭环**）。**本台账无新增 / 无状态变更的 BUG**（该口径冲突自始未立 BUG，功能面事实与结论不变：非空确定性覆盖项在生产一律 fail-fast）。 |
| 失效标记（`60-review` **v3 → v4** 触发复核，2026-09-17） | 上游 `docs/60-review.md` 更新至 **v4**（REV-20 经 CHG-15 修复后的**定向复审**：格式门禁 45 → **0**、exit 2 → **exit 0**，v4 判 **已闭环**）→ 按失效传播矩阵**人工复核 + 更新失效标记**：v4 仅更新 **REV-20** 一项，其余 **19 项维持 v3 判定**，**v4 轮新增发现 0 项**、**0 致命 / 严重未闭环**；REV-20 系**代码格式门禁发现、自始未登记为本台账 BUG**（记于 `docs/52-qa-report.md` §3 OBS-10，本轮已标记 **已消除**；本角色同日独立复跑四个项目 `dotnet format whitespace --verify-no-changes` **全部 exit 0**，证据 `tests/e2e/logs/format-gate-rerun-20260917.log`）→ **本台账无新增 / 无状态变更的 BUG；v4 未改变影响 51 结论的任何判定。** |
| 证据引用勘误（2026-09-17） | 核验发现 1 处被引证据文件名在磁盘不存在，已修正：`tc30-regression-20260917-151508.log` → **`…-151255.log`**（§0 R2 / §1 BUG-03 / §0 回归小结 共 3 处；`tests/e2e/logs/` 位于 Playwright `outputDir` 之外、不受运行前自动清空影响 → 判定为笔误，非文件被清理）。 |
| 失效标记（上游 `30-architecture` v4 / `40-changelog` CHG-16+17 / `60-review` v5 变更，2026-09-17 R7 回写） | 上游已变更：`docs/30-architecture.md` **v3 → v4**（D-08 整事务重试耗尽终态 `500` → **`1001`（HTTP 200）**、API-07 语义显式化）、`docs/40-changelog.md` 新增 **CHG-16**（BUG-04 / BUG-05 修复）与 **CHG-17**（用户裁决：`MaxTransactionAttempts` 3 → 2、`CommandTimeout` 30 → 60）→ 按 `docs/artifacts.md` §5 矩阵回写本台账：**BUG-04 / BUG-05 经 R7 实跑复验 → 均置 `已闭环`**（逐条证据见 §2 各 BUG 的「回归验证」块）；`docs/60-review.md` **v4 → v5** 定向复审：新增 **REV-21（建议级、`未闭环`）**，系 **CRITICAL 告警文案把「尝试 2 次」写成「重试 2 次」**（与 D-08「重试 1 次」口径错位）——属**审查问题项、非测试发现的缺陷**，按纳入规则（仅**致命 / 严重**必纳）**不登记为本台账 BUG**；其状态与状态机行 0.5 的判定口径由本角色在 `docs/52-qa-report.md` §2「60 审查项状态」节单列（结论：REV-21 不在 51 台账内，**不计入行 0.5**；如实标注供主对话向用户上报）。**【本轮更新（2026-09-17）：`docs/60-review.md` 已至 v6 —— REV-21 经 CHG-18 修复后转 `已闭环`（v6 §A：21 项 = 已闭环 16 / 已接受（用户决断）5 / 未闭环 0；致命 / 严重未闭环 0 项）；本台账无对应 BUG 新增或状态变更（REV-21 自始不在本台账内）。】** |
| 失效标记（R7 回归回写，2026-09-17） | 本批**不改动任何上游契约**；回归执行 = **TC-56b / TC-56c / TC-48c → 3/3 PASS**（第 3 轮回归，命令 / 原始输出 / 关键实测值见 §2 各 BUG 的「回归验证」块）。**BUG-04（建议）、BUG-05（一般）状态 → `已闭环`；当前未闭环缺陷 0 条；无致命 / 严重缺陷。** |
| 失效标记（`40-changelog` **CHG-18** + `60-review` **v6** 触发同步回写，2026-09-17） | 上游两处变更：`docs/40-changelog.md` 新增 **CHG-18**（REV-21 修复：终态 CRITICAL 告警文案计数口径「尝试次数 → 重试次数」+ 守护性断言）、`docs/60-review.md` **v6**（CHG-18 定向复审：**REV-21 → `已闭环`**；现行 21 项 = 已闭环 16 / 已接受（用户决断）5 / **未闭环 0**；**致命 / 严重未闭环 0 项**）。→ 按 `docs/artifacts.md` §5 矩阵回写本台账：**本台账无新增 BUG、无 BUG 状态变更**（REV-21 属 `60-review` 审查问题项、按纳入规则未登记为 BUG，其状态同步见本元信息块上一行与 `docs/52-qa-report.md` §2「60 审查项状态」）。**BUG-04 / BUG-05 状态维持 `已闭环` 不变**（本轮无对应回归动作）；**未修改本文件任何既有 BUG 记录**（历史证据按原样保留）。 |
| 写入者 | test-executor |
| 状态 | **回归轮完成（2026-09-17）**：**已执行 98/98、未执行 0 条**——最后一批 **R6（破坏性批：TC-56b / TC-56c）2/2 PASS**（见 §0 R6 与 `docs/52-qa-report.md` §1.6.6）。首轮 85/98 → R1–R5 补做 11 条 → **R6 补做 2 条**，覆盖缺口全部清零（§4 清单为空）。回归轮结论：BUG-01 / BUG-02 / BUG-03 **`已闭环`**；**新增 BUG-04（建议，未闭环）** 与 **BUG-05（一般，未闭环；R6 执行 TC-56b 时暴露，见 §2）**；待确认项 **QX-E-07 已裁决（用户决断）并闭环**（50 v3 + 断言修订 + 复跑 3 passed）；**QX-E-05 已消解**（统计抽样已按不改 `src/` 的方式执行完毕，无需再授权）。首轮基线见本文件 v1 冻结内容。**R7（CHG-16 / CHG-17 修复后回归，2026-09-17 18:27–18:30）**：TC-56b / TC-56c / TC-48c **3/3 PASS**；**BUG-04、BUG-05 均 → `已闭环`**（验证方式逐条见 §2）；**未闭环缺陷 0 条**；**（R7 时点）唯一未闭环建议级项 = `docs/60-review.md` v5 的 **REV-21**（审查项，非本台账 BUG，见 52 §2「60 审查项状态」）；【2026-09-17 更新：REV-21 已经 **CHG-18** 修复 → `已闭环`（60 v6 §A），见本元信息块「CHG-18 / v6 同步回写」失效标记行】** |
| 失效标记 / 复核结论（R6 破坏性批回写，2026-09-17） | 本批**不改动任何上游契约**（`50` v3 未变、`30-architecture` 未变、`60-review` v4 未变），故无失效传播；本文件与 `docs/52-qa-report.md` 为同批同源回写。**R6 新增 BUG-05（一般，`已闭环`）**，其关联 TC 为 **TC-56b**（该用例本身判定 **PASS**——回滚一致性 5/5 通过；BUG-05 系用例执行过程**暴露**的产品问题，**非**用例判据失败）。**最高严重程度由「一般」（首轮 BUG-01）保持为「一般」**；当前未闭环：**BUG-04（建议）+ BUG-05（一般）**；**无未闭环致命 / 严重**。 |
| 执行侧补验标签勘误（2026-09-18） | 核验发现 **2 个执行侧标签被当作契约用例 ID 引用**；两者**均非 `docs/50-testcases.md` 定义的用例，属执行侧补验标签**（不新增用例、不改 50）：① **`TC-13b`** —— 定义位置 = `tests/e2e/api/api-round2.mjs` 的 `rec('TC-13b', …)` 调用；所测内容 = **TC-13** 的契约分支「锁定按提交用户名计数，**对不存在的用户名同样生效**（防枚举）」（`docs/50-testcases.md` TC-13 期望结果列原文）→ 对应契约用例 = **TC-13**；证据位置 = `tests/e2e/logs/api-round2-20260917-145013.log` 与 `tests/e2e/logs/api-round2-regression-20260917-151229.log` 中的 `[PASS] TC-13b ::` 记录（同文件末尾 JSON 明细 `"tc": "TC-13b"`）。② **`TC-39b`** —— 定义位置 = `tests/e2e/qa.spec.ts` 的 TC-39 用例**标题**；经**全量检索 + 读用例体**确认**无对应独立分支**（用例体 `console.log` 亦只打 `TC-39 ::`）→ 对应契约用例 = **TC-39**；**处置 = 订正标题**（`TC-39 / TC-39b 确定命中：…` → `TC-39 确定命中：…`，去掉悬空标签），历史运行日志中的旧标题属历史快照、**不回改**。本行为**引用勘误**（不涉契约内容、不改任何用例判定），按 `docs/artifacts.md` §4 第 5 条**只登记、不升版**；`docs/52-qa-report.md` **同批同源**登记。 |
| 阻断标注（2026-09-18） | §3 观察项表**新增末列「阻断 / 非阻断」**（结构化取值；判别口径 = `docs/artifacts.md` §3.1 / `docs/state-machine.md` D2 第 2 项：**环境 / 证据 / 工具链类默认按「阻断」登记**，仅在「该观察项不影响任何结论的成立」时才标 `非阻断`，逐条理由写在本表各行「判定依据」列内）。本文件序列 9 条**实测取值：阻断 6**（OBS-03 / OBS-05 / OBS-06 / OBS-07 / OBS-08 / OBS-11）、**非阻断 3**（OBS-01 / OBS-02 / OBS-04）；同批 `docs/52-qa-report.md` §3 同源登记（其序列 11 条：阻断 6 / 非阻断 5）。属 §4 第三类**台账类回写（阻断标注）→ 按 §4「不升版」，仅登记**（改动哪一类 / 何时 / 依据哪条规则 = 本条）；**OBS 编号、编号勘误块与历史行文一律不回改**，本行**不改变任何 BUG 状态与结论**。 **（2026-09-20 三态批次补充）**：本条所列判别口径已随用户 2026-09-20 裁决扩为**取值闭集（三态）= `阻断` / `非阻断` / `已接受（用户决断）`**（第三态 = **影响结论成立但用户已就该观察项逐项裁决接受（本轮不修）**，与 `非阻断` 语义相反；口径单点定义 = `docs/artifacts.md` §3.1 / `docs/state-machine.md` D2 第 2 项，第三态**不计入 D2 阻断**）；本文件当前分布与 4 行改判逐条见本头部三态批次登记行与 §3 各行的改判段。**本条前述「实测取值：阻断 6 / 非阻断 3」为 2026-09-18 批的历史快照，按 13.4 不回改。** |
| 引用勘误（2026-09-18 · 行号引用实测复核） | 按 `docs/development-spec.md` **13.4** 对**本文件**全部「文件名:行号」引用做**逐处磁盘实测**（2026-09-18）。**已失效 / 已漂移的引用如下（原引用 → 实测 → 处置；按 13.4 不回改历史正文，只在此登记）**：① `error-codes.md:4` 的引文「`4xx/5xx` 段与 HTTP 状态码对齐」—— 实测该行现为「使用规则」条（系统码 `0` 为成功码、`0–500` 段与 HTTP 状态码对齐、**唯一例外是 `429` 限流：HTTP 429 而响应体 `code=1001`**），**引文措辞在磁盘不存在** → **订正引文**（稳定 ID = `docs/error-codes.md` §使用规则）。② `docs/30-architecture.md:598`（§2 BUG-01）—— 实测该行现为 **D-09「备选与取舍」**；`409` 的承接登记现位于 **D-12「登录失败不得用 401 表达」的「各状态与 HTTP 的分工」段**（实测 2026-09-18，现 `:623`）→ 订正为 **D-12**（稳定 ID）。③ `src/frontend/src/views/draw/DrawView.vue:85-89` / 同文件 `:55`（§2 BUG-04「实现侧」）—— 实测该区间现为 **CHG-16 修复后代码**（`:55` 为空白行、`:87-97` 为重拉失败兜底分支），**原引用对象已被修复取代** → 订正为「**CHG-16 修复前位置**」+ 稳定 ID `BUG-04`（历史行文不回改）。④ `docs/30-architecture.md:544`（§2 BUG-05）—— 实测该行现为 **D-07（奖池 DTO）**段；所引 **D-08「异常与重试」** 现位于 **`:568`** → 订正为 **D-08**（稳定 ID）。⑤ `DrawService.cs:109`（§2 BUG-05）—— 实测该行现为 `return cached;`；所引「明文点名处理死锁 1213 / 锁等待超时 1205」的注释现位于 **`:113-114`** → 订正为稳定 ID（**D-08 重试判定**）。⑥ `DrawService.cs:117-130 / 185 / 237`（§2 BUG-05「代码侧」）—— 实测经 CHG-16 后三处均已漂移；**写点 ⑥ / ⑦ / ⑧** 现位于 **`:198-207` / `:211-218` / `:221`**（事务提交现 `:223`）→ 订正为稳定 ID（`BUG-05` / `CHG-16` 的写入点与事务范围）。⑦ `docs/30-architecture.md:502`（待确认清单 QX-E-07）—— 实测该行现为 **D-05（时区）**段；所引 **D-06 散文**现位于 **D-06 段（`:515` 起；配置键表 `:519-524`、校验规则 `:526`）**→ 订正为 **D-06**（稳定 ID）。⑧ `docs/50-testcases.md` **v2** `:264` / **v3** `:268`（QX-E-07）—— 实测 **50 当前版本 v4**，`TC-76` 行现位于 **`:290`** → 订正为 **TC-76**（稳定 ID）。⑨ `docs/30-architecture.md:966`（QX-E-07）—— 实测 **RSK-04 现位于 `:991`**，且原引「不含该节」措辞在当前磁盘内容中**不存在** → 订正为 **RSK-04**（稳定 ID）。⑩ `docs/30-architecture.md:495-500`（QX-E-07）—— 实测 D-06 配置键表现位于 **`:519-524`** → 订正为 **D-06 配置键表**（稳定 ID）。**证据目录（非行号）**：§3 OBS-08 所引 `tests/e2e/.artifacts/` 下「本轮 3 个失败目录」—— 实测（2026-09-18）**该目录已不存在**（Playwright 运行前自动清空，与本文件 OBS-08 记录的事实一致）→ 该行证据强度按**旁证**计、**当前不可核验**（`docs/52-qa-report.md` 同批同源登记）。**其余引用经同一批实测仍有效（实测 2026-09-18）**：`src/frontend/src/utils/messages.ts:17`、`Domain/Exceptions/TransientDataException.cs:4`、`Application/Common/ErrorCodes.cs:10`、`Infrastructure/Data/MySqlErrors.cs:33`、`Infrastructure/Repositories/WinningRecordRepository.cs:25`、`Api/Program.cs:76-78`、`Infrastructure/Tokens/JwtTokenService.cs:40-42`、`Application/Services/AuthService.cs:283-285`、`WinningRecordsView.vue:199-202` / `:120-127`、`tests/e2e/logs/stats-instance-20260917-164034.log` **行 34618–34725**（经 `tests/e2e/logs/tc56b-instance-excerpt-20260917-170943.log` 引用；该源日志文件在磁盘存在）。**另注**：`src/` 行号引用**同日正由 engineer 清理**（清除代码注释内的行号锚点），本行 src 实测值截至 **2026-09-18**；其落盘后若行号再次漂移，**以稳定 ID 为准**。`docs/52-qa-report.md` §3 OBS-08 行同批做了**表格可机械解析性修正**（引文中的转义竖线 → 全角 `｜`，见该文件「引用勘误」行）；本文件的同型引文不是 OBS 行、不触发护栏列索引问题，按原样保留。本行属**引用勘误**（不涉契约内容、不改任何判定），按 §4 第 5 条**只登记、不升版**。 |
| 台账回写 / 新增观察项登记（2026-09-20） | §3 观察项表**新增 `51:OBS-12`**（集成测试夹具 Redis 覆盖值「半截」缺陷；运行期复现与全量证据见 §3 该行 + 表下问答块；`docs/52-qa-report.md` 头部 2026-09-20 行**同批同源登记**）。本行登记属 `docs/artifacts.md` §4 第三类**台账类回写（新增观察项登记）→ 按 §4「不升版」，仅登记**（改动哪一类 = §3 观察项表 + 本头部两行 + 冻结时间行 ⑤；何时 = 2026-09-20，落盘时刻实测见冻结时间行 ⑤；依据规则 = `docs/artifacts.md` §3.1（观察项登记要求）+ §4（台账类回写不升版） + `docs/state-machine.md` D2 第 2 项）。**不改变任何缺陷状态与既有结论**：BUG-01…05 维持 `已闭环`、未闭环缺陷 0 条、最高严重程度「一般」、§4 未执行用例 0 条（98/98）均不变；§3 观察项分布由 9 条（阻断 6 / 非阻断 3）变为 10 条（**阻断 7 / 非阻断 3**）。 |
| 失效标记 / 复核结论（`60-review` 台账回写轮同步 + `60:OBS-32` 的 e2e 扇出复核，2026-09-20） | 上游 `docs/60-review.md` 本轮为**台账回写轮**（**未升版**，判定结论未变：21 项 = 已闭环 16 / 已接受（用户决断）5 / 未闭环 0）：`60:OBS-01` 已处置；`60:OBS-05` / `60:OBS-08` / `60:OBS-18` / `60:OBS-31` 已闭环；`60:OBS-32` **维持阻断 / 未闭环**；新增 `60:OBS-33`（非阻断）。**本角色对 51 的同步复核结论**：本文件此前对上述各项**无任何引用**（全量检索 `60:OBS` **零命中**）、REV 统计引用与现行一致 → **既有行无需回写**。**接手项（`60:OBS-32` 的唯一残余 = `tests/e2e/**` 扇出）复核完成**：判定 = 「有理由的未复跑」闭集 ①「变更不触及断言面」——证据强度**版本控制级**（`git show 6cc4c10` 实测：6 个受控文件共 12 行改动 = 10 行纯注释 + 1 行行尾注释（该行可执行部分逐字节未变）+ 1 行用例标题字符串，**断言行 0 处**）；`node --check` 实测 5 个 `.mjs` **exit 0 ×5**，并以已知坏样本 **exit 1** 佐证检查非空转；检索实测 = `tests/e2e` 源码面「行号形态」引用在 HEAD **零命中**、在父提交 `6cc4c10^` **8 处命中**（正对照）。产物目录（绝对路径）：`D:\AI test\AI-Agents-workflow-Engineer-Rules\tests\e2e\_tmp-obs60-32-e2e-fanout-20260920-01`。`60:OBS-32` 的解除条件（test-executor 完成 e2e 面判定 / 复跑）**本角色侧已满足**；其台账状态回写归 code-reviewer / 主对话。**未重开**用户已裁决项（REV-10 / REV-11 / REV-12 / REV-16 / REV-18）与 CHG-17 交付残留（前端 15s vs 后端 100s）。**另如实登记一处差异（不自行改写）**：`docs/52-qa-report.md` §2 阻断面清单的「阻断 `52:OBS` 共 6 条」口径未含 60 侧现行阻断型 `60:OBS-32`，亦不含本批 `51:OBS-12`；该处受本轮调度约束保护，本角色未改，差异登记见 52 头部 2026-09-20 行。本行属**台账类回写（状态同步复核）→ 按 §4 不升版，仅登记**。 |
| 台账回写（`51:OBS-12` 闭环 + 新增 `51:OBS-13` / `51:OBS-14`，2026-09-20 Wave D） | §3 观察项表三处变更：① **`51:OBS-12` → `已闭环`**（2026-09-20 Wave D 第 1 轮独立复现验证：判别轮与对照轮各 `Failed: 0, Passed: 43`、三库 marker 探针逐库符合期望、守护断言非空转经阴性样本实测 43 例全红 + 逐字节还原复绿；原始读数与证据路径见 §3 该行「判定依据」列末段）；② **该行末列取值由「阻断」重标「非阻断」**（口径 = `52:OBS-10` 先例：修复经独立复现闭环 → 不再影响任何结论的成立）；③ 新增 **`51:OBS-13`**（集成用例 `Refresh_WithoutCookie_Reports1204` 的预置条件与用例名不符；归类 `执行侧`、非阻断）与 **`51:OBS-14`**（集成套件结构上无法覆盖「应用在 Redis 降级下的行为」；归类 `执行侧`、非阻断）。**分布变化（实测计数）**：10 条（阻断 7 / 非阻断 3）→ **12 条（阻断 6 / 非阻断 6）**。本行属 `docs/artifacts.md` §4 第三类**台账类回写**（OBS 状态回写 / 阻断标注 / 新增观察项登记）→ 按 §4「不升版」，仅登记（改动哪一类 = §3 观察项表 + 本头部两行 + 冻结时间行 ⑥；何时 = 2026-09-20，落盘时刻实测见冻结时间行 ⑥；依据规则 = `docs/artifacts.md` §3.1（观察项登记要求）+ §4（台账类回写不升版）+ `docs/state-machine.md` D2 第 2 项）。**不改变任何缺陷状态与既有结论**：BUG-01…05 维持 `已闭环`、未闭环缺陷 0 条、§4 未执行用例 0 条（98/98）均不变。 |
| 失效标记 / 复核结论（`tests/integration` 夹具变更的 §5 扇出复核 + 「矩阵缺行」事实，2026-09-20 Wave D） | 上游 `docs/40-changelog.md` 新增 **CHG-20**（`tests/integration/**` 两个文件：`IntegrationFixture.cs` 修复 + `LuckyDrawApiFactory.cs` 的 `<summary>` 声明）→ 按 `docs/artifacts.md` §5 矩阵逐行核对：矩阵**没有 `tests/**` 变更行**；「`30-architecture 变更`」行仅列 `tests/unit/**` 与 `tests/e2e/**`、**未含 `tests/integration/**`**（而 `docs/30-architecture.md` 自身的变更说明把 `tests/integration/**` 列为其下游）⇒ 按矩阵字面，本次改动不触发任何下游复核。**本角色独立复核的检索取证（2026-09-20 实测、本批写入前；数为命中行数）**：`tests/integration` 字面 = `40-changelog.md` 46 / `60-review.md` 9 / `30-architecture.md` 4 / `50-testcases.md` 1 / `51-defects.md` 1（即本文件的 `51:OBS-12` 行）/ **`52-qa-report.md` 0**；`LUCKDRAW_TEST_REDIS` = `40-changelog.md` 6 / `60-review.md` 2 / `51-defects.md` 1；`IntegrationFixture` = `40-changelog.md` 6 / `60-review.md` 5 / `51-defects.md` 1；`LuckyDrawApiFactory` = `40-changelog.md` 7 / `60-review.md` 3 / `51-defects.md` 1（检索产物 = 证据目录 `tests/integration/_tmp-obs12-close-20260920-01` 的 21 / 22 号文件）。**复核结论（无需下游事实改写）**：① 以被改夹具为证据面的**现行**事实 = 本文件 `51:OBS-12`（本轮闭环）；② `docs/60-review.md` 记录的集成运行读数（38 / 39 / 43，均为其轮次的历史快照，按 13.4 不回改历史记录）与 `docs/40-changelog.md` 各 CHG 的集成读数**不需要回写** —— 默认路径（无覆盖值）行为等价（对照轮本角色实测 43/43、清理靶 = db1），且本轮已在修复后终态代码上复跑 43/43；③ `30-architecture.md` / `50-testcases.md` 的命中均为分区归属 / 实施路径声明（非运行读数、无夹具依赖）；④ `52-qa-report.md` 零命中 → 其事实与结论不以集成套件为证据面。**「矩阵缺行」事实（驻留主对话裁决）**：§5 无 `tests/**` 变更行、且「30-architecture 变更」行漏 `tests/integration/**` → 测试侧改动可合法地**不触发任何下游复核**；本角色不得改 `docs/artifacts.md`，是否补行（及是否同步断言）由主对话裁决。本行属**台账类回写（复核结论登记）→ 按 §4 不升版**。 |
| 台账回写 / 头部元信息取值订正（阻断型 OBS 行补对侧编号标注 + 冻结时间行 ⑥ 取值订正，2026-09-20 B 批） | ① §3 观察项表 **6 条阻断型行**（`51:OBS-03` / `51:OBS-05` / `51:OBS-06` / `51:OBS-07` / `51:OBS-08` / `51:OBS-11`）在**描述列末尾**补对侧编号标注，依次为 `52:OBS-03` / `52:OBS-05` / `52:OBS-06` / `52:OBS-07` / **`52:OBS-09`** / `52:OBS-11` —— `51:OBS-08` 的对侧是 **`52:OBS-09`**（同一 Playwright `outputDir` 事实、编号跨文件**错位**；**非** `52:OBS-08` —— 后者是另一件事：TC-60 用例缺陷、非阻断）。形态与判据 = `docs/artifacts.md` §3.1「门禁输入的阻断面 = 事实级并集」；两侧**互指**（对侧行同批回写 `↔ 51:OBS-nn`），由护栏 T31 机检。② 冻结时间行 ⑥ 的取值由 `2026-09-20T11:43:49` 订正为 `2026-09-20T11:44:49`（证据链 = 本角色自报 + 主对话复核文件 mtime；**按 §4「落盘证据只认 git 提交时刻」，文件 mtime 不作落盘证据**；承载提交 hash + 提交时间 = `60b6cf4`（全量 `60b6cf411a3fef20501cdfd145ea3ffb4b3bf31f`）/ `2026-09-20T12:56:37+08:00`（2026-09-20 承载提交回填；主对话 `git log` 实测）；本回填自身由**下一次提交**承载）。本行属 `docs/artifacts.md` §4 第三类**台账类回写（阻断标注补对侧编号）+ 头部元信息取值订正 → 按 §4「不升版」，仅登记**（改了哪一类 / 何时 / 依据哪条规则 = 本条；落盘时刻实测见冻结时间行 ⑦）。**不改任何阻断列取值、不改任何 BUG 状态与判定结论**：BUG-01…05 维持 `已闭环`、未闭环缺陷 0 条、§3 观察项分布维持 12 条（阻断 6 / 非阻断 6）。 |
| 台账回写（承载提交 hash 回填，2026-09-20） | 本批 = **承载提交 hash 回填**（`docs/artifacts.md` §4 第三类台账类回写）：对冻结时间行 ④ / ⑤ / ⑥ / ⑦ 四处「承载提交尚未产生」表述逐处**追加**回填补记（原句逐字保留），并对两处显式占位（冻结时间行 ⑥ 的取值订正、上一条登记行的 ②）**原地填值**。回填值：2026-09-20 落盘批（⑤ / ⑥ / ⑦）= `60b6cf4`（全量 `60b6cf411a3fef20501cdfd145ea3ffb4b3bf31f`、提交时间 `2026-09-20T12:56:37+08:00`；来源 = 主对话 `git log` 实测）；2026-09-18 落盘批（④）= `6cc4c10`（全量 `6cc4c10e97c946fbfe5a148c82d0bd5c94c1385e`、提交时间 `2026-09-18T17:57:11+08:00`；来源 = 本角色 `git log -S` pickaxe 实测：该批落盘文本由 `6cc4c10` 引入、`60b6cf4` 未改动该串）。落盘时刻实测 = `2026-09-20T13:00:56`（python datetime.now()，本机 `date` 不可用）。本行属 §4 第三类台账类回写（头部元信息回填 / 登记）→ 按 §4「不升版」，仅登记（改了哪一类 / 何时 / 依据哪条规则 = 本条）；**冻结时间的取值本身未重算、未改动**。**不改任何阻断列取值、不改任何 BUG 状态与判定结论**：BUG-01…05 维持 `已闭环`、未闭环缺陷 0 条、§3 观察项分布维持 12 条（阻断 6 / 非阻断 6）。本回填自身由**下一次提交**承载。 |
| 台账回写（三态批次：4 条阻断型观察项改判 `已接受（用户决断）` + `51:OBS-08` 修复进展登记 + `QX-E-01` / `QX-E-02` 状态更新，2026-09-20 C 批） | 依据 = **用户 2026-09-20 裁决**（经主对话调度指令转达）与 `docs/artifacts.md` §3.1 / `docs/state-machine.md` D2 第 2 项**三态闭集**（`阻断` / `非阻断` / `已接受（用户决断）`；第三态 = **影响结论成立但用户已就该观察项逐项裁决接受（本轮不修）**，**不计入 D2 阻断**）。本批四处变更：① **§3 观察项表 4 行末列 `阻断` → `已接受（用户决断）`**（`51:OBS-03` / `51:OBS-06` / `51:OBS-07` / `51:OBS-11`；逐行在「判定依据」列追加**接受人 / 理由 / 补丁计划**三要素与裁决出处，结构化可机械提取）；② **`51:OBS-08` 行追加修复进展登记**（对侧同一事实 = `52:OBS-09`；修复已落地 `playwright.config.ts`，**取值维持「阻断」、不标 `已闭环`** —— 按 `51:OBS-12` 先例，闭环须经独立复现验证，留终批）；③ **待确认清单 `QX-E-01` → 已裁决（用户决断）**（维持契约空白 + 记入 `docs/52-qa-report.md` §2 残留清单 + 补丁计划）、**`QX-E-02` → 已消解**（与 `51:OBS-03` **事实级去重**，不再重复计入 D2 第 1 项）；④ 头部新增本行。**分布变化（实测计数）**：12 条 = **阻断 2 / 非阻断 6 / 已接受（用户决断）4**（改前 = 阻断 6 / 非阻断 6）。本行属 `docs/artifacts.md` §4 第三类**台账类回写**（OBS 状态改判 / 阻断标注 / 待确认项状态更新 / 修复进展登记）→ 按 §4「不升版」，仅登记（改动哪一类 = §3 观察项表 + 待确认清单 + 本头部两行 + 冻结时间行 ⑧；何时 = 2026-09-20，落盘时刻实测见冻结时间行 ⑧；依据哪条规则 = `docs/artifacts.md` §3.1 + `docs/state-machine.md` D2 第 2 项 + 用户 2026-09-20 裁决）。**不改变任何 BUG 状态与既有判定**：BUG-01…05 维持 `已闭环`、未闭环缺陷 0 条、§4 未执行用例 0 条（98/98）均不变。**（2026-09-20 终批前半段同步）**：本行所记分布（12 条 = 阻断 2 / 非阻断 6 / 已接受（用户决断）4）为该批**历史快照**，按 13.4 不回改；本批回写后**实测现行分布 = 12 条 = 阻断 1（`51:OBS-08`）/ 非阻断 7 / 已接受（用户决断）4**（改前 = 阻断 2 / 非阻断 6 / 已接受（用户决断）4；`51:OBS-05` 闭环重标「非阻断」），明细见本头部本批登记行与 §3 表下问答块末段。 |
| 台账回写（终批前半段：`51:OBS-05` 闭环 + `51:OBS-08` 修复进展登记 + 冻结时间行 ⑨，2026-09-20） | 依据 = `docs/60-review.md` v7 §J 对本批闭环对象的**独立复现**（集成 45 / 0 / 0 + 负控 N1–N5 全捕获）+ 本角色对磁盘证据链的核验（未改任何上游契约）。本行按 `docs/artifacts.md` §4 第三类**台账类回写（OBS 状态回写 / 进展登记）→ 按 §4「不升版」，仅登记**（改动哪一类 = §3 观察项表两行 + §3 表下问答块 + 本头部两行 + 冻结时间行 ⑨；何时 = 2026-09-20（落盘时刻实测见冻结时间行 ⑨）；依据哪条规则 = `docs/artifacts.md` §3.1 + `docs/state-machine.md` D2 第 2 项 + `docs/development-spec.md` 13.4）。**本批后本表实测分布 = 12 条 = 阻断 1（`51:OBS-08`）/ 非阻断 7 / 已接受（用户决断）4**（改前 = 阻断 2 / 非阻断 6 / 已接受（用户决断）4）；**跨侧现行阻断事实 = 1 条**（Playwright `outputDir` 每次运行前自动清空；`51:OBS-08` 与 `52:OBS-09` 为同一事实、未闭环）。**不改变任何 BUG 状态与既有判定**：BUG-01…05 维持 `已闭环`、未闭环缺陷 0 条、§4 未执行用例 0 条（98/98）均不变。本批取证目录（绝对路径，留存不删）：`D:\AI test\AI-Agents-workflow-Engineer-Rules\_tmp-close-final-half-20260920-01`（改前副本 / 编辑断言输出 / CRLF 核验读数 / 门禁读数）。 |
| 失效标记 / 复核结论（`40-changelog` **v3 → v4（CHG-21）** 触发，2026-09-20 终批后半段） | 上游 `docs/40-changelog.md` 升至 **v4**（新增 **CHG-21**：应用日志落盘为文件（Serilog `File` sink）+ 守护用例 + 负向验证）—— 本文件此前**仅**在 §3 `51:OBS-05` 行登记其闭环，**头部无失效标记 / 复核结论行**。→ 按 `docs/artifacts.md` §5 矩阵逐行核对：「`40-changelog 变更` → `51-defects`、`52-qa-report`、`60-review`」行**命中本文件** ⇒ 须补失效标记 + 复核结论（**本行即补**）。**复核方法（本角色实测，2026-09-20 终批后半段）**：① 全量检索本文件 `CHG-21` 命中 = §3 `51:OBS-05` 行（修复侧证据）→ 维持其闭环判定；② 判据面复核 = `tests/e2e/api/api-suite.mjs`（TC-81 实现）的失真陈述已按 CHG-21 §9 处置（改前 / 改后 / 理由见 `docs/52-qa-report.md` §1.6.8），并新增**落盘日志正文扫描**：扫描面 5 个（存在 3 个共 **3 864 133 B**、12 个闸门模式命中 **0**、扫描器自证通过；读数见 `docs/52-qa-report.md` §1.6.8）；③ CHG-21 §9 三项 e2e 侧移交逐项复核：`api-suite.mjs` 陈述已修订（本批）；`<发布目录>/logs/` 落点当前不存在（`tests/e2e/deploy/` 系 `.gitignore:46` 的构建产物目录、当前未发布 → TC-81 每次运行如实记「不存在（未扫描）」，登记见 `51:OBS-15`）；GBK 控制台捕获辅助函数与文件侧 UTF-8 的区分不变（新扫描器按字节面匹配、不经 GBK 解码）。**复核结论（无新增 BUG、无状态变更）**：CHG-21 未推翻本文件任何既有判定；其在本文件的可核验面（`51:OBS-05` 闭环证据）维持。本行属**台账类回写（失效标记 / 复核结论登记）→ 按 §4 不升版**。 |
| 失效标记 / 复核结论（`60-review` **v7 → v8** 触发复核，2026-09-20 终批后半段） | 上游 `docs/60-review.md` 升至 **v8**（**依据用户 2026-09-20 裁决**（经主对话转达）：`60:REV-22` / `60:REV-23` / `60:REV-24` 与 `60:OBS-34` 均 **`已闭环`** —— 属**判定结论变化**，按 `docs/artifacts.md` §4 升版）→ 按 §5 矩阵「`60-review 变更` → `51-defects`、`52-qa-report`」行**人工复核 + 更新失效标记**（v8 头部第 ④ 条明示该下游同步义务）。**v8 关键读数（本角色实测复核：Read / Grep / 主检自跑）**：现行 **24 项 = 已闭环 19 / `已接受（用户决断）` 5 / 未闭环 0**；**致命 / 严重未闭环 0 项**；**60 侧现行阻断型 `60:OBS` = 0**；三项新增 REV 均一般级（22 = CHG-21 §5 论证句失真；23 = `.gitignore` 根锚定 `/logs/` 覆盖不到开发态落点；24 = CHG-21「套件不可运行」陈述失真）。**本角色对 REV-23 修复面的独立复跑**：`git check-ignore -v src/backend/src/LuckyDraw.Api/logs/luckydraw-20260920.log` → 命中 `.gitignore:35` 新增锚定规则（实测 2026-09-20）。**主检自跑（本角色）**：`python tools/check-config.py` = **EXIT 0 / 共 385 项检查 / 全部 PASS**（校验基线：HEAD `5491573`（全量 `5491573172df54d06f508a3fb27c71a77ef0b472`）/ 工作区脏 29 个文件；读数为本批写入前基线，写入后末次读数见 `docs/52-qa-report.md` §1.6.8）。**复核结论**：v8 未推翻本文件任何既有判定 —— 三项闭环的落点均在 `docs/40-changelog.md` CHG-21 的举证句与 `.gitignore` 锚定（**不在 51 台账内**）→ **本文件无对应 BUG 新增或状态变更**；v7 曾声明的 `60:OBS-32` 唯一残余（`tests/e2e/**` 扇出）此前已由本角色复核完成（见本头部 2026-09-20 行）；对 D2 第 4 项的影响（未闭环 REV = 0）记于 `docs/52-qa-report.md` §2。本行属**台账类回写（失效标记 / 复核结论登记）→ 按 §4 不升版**。 |
| 台账回写（`51:OBS-08` 闭环 + 新增 `51:OBS-15` + §3 表下同步段 + §1 `60` v8 追记，2026-09-20 终批后半段） | 依据 = `docs/role-protocol.md` §7 第 1 条（每轮 `PLAYWRIGHT_OUTPUT_DIR` 指向本轮独立目录 = `51:OBS-08` / `52:OBS-09` 闭环的**成立条件**）+ `.claude/agents/test-executor.md` 第 6 条 + 本角色实测（两轮独立目录实跑 + 跨轮读回；读数见 §3 `51:OBS-08` 行回写块与证据目录 `D:\AI test\AI-Agents-workflow-Engineer-Rules\tests\e2e\_tmp-obs09-persist-20260920-01`）。本批变更 = §3 观察项表 `51:OBS-08` 闭环回写（状态 → `已闭环`、末列「阻断」→「非阻断」）+ 新增 `51:OBS-15` + §3 表下问答块追加「终批后半段同步」段 + §1 最高严重程度行追加 `60` v8 追记 + 本头部三行。**分布变化（实测计数）**：12 条（阻断 1（`51:OBS-08`）/ 非阻断 7 / 已接受（用户决断）4）→ **13 条（阻断 0 / 非阻断 9 / 已接受（用户决断）4）**；**跨侧现行阻断事实 = 0 条**（`51:OBS-08` ≡ `52:OBS-09` 同一事实、本批闭环）。本行属 `docs/artifacts.md` §4 第三类**台账类回写 → 按 §4「不升版」，仅登记**（改动哪一类 = §3 观察项表 + 表下段 + §1 一行 + 本头部三行 + 冻结时间行 ⑩；何时 = 2026-09-20（落盘时刻实测见冻结时间行 ⑩）；依据哪条规则 = `docs/artifacts.md` §3.1 + §4 + `docs/state-machine.md` D2 第 2 项 + `docs/role-protocol.md` §7 第 1 条）。**不改变任何 BUG 状态与既有判定**：BUG-01…05 维持 `已闭环`、未闭环缺陷 0 条、§4 未执行用例 0 条（98/98）均不变。 **末次主检读数（本批全部结构性落盘后实测）**：`python tools/check-config.py` = **EXIT 0 / 共 385 项检查 / 全部 PASS**（校验基线：HEAD `5491573` / 工作区脏 31 个文件；输出见 `docs/52-qa-report.md` §1.6.8）。 |

---

## 0. 回归轮进度（2026-09-17，进行中）

**本轮授权**：主对话转达用户裁决 —— BUG-01 **返工**（engineer 已修 CHG-14）；BUG-02、BUG-03 **一并修复**（CHG-14）；13 条覆盖缺口**授权补做**（可用环境变量另起实例改运行配置，**不得改 `src/`**）。

| 批次 | 内容 | 状态 |
| --- | --- | --- |
| R1 | 回归 TC-08 / TC-17（BUG-01）、TC-63（BUG-02）：`node tests/e2e/api/api-round2.mjs` | **已完成（2026-09-17 15:12）**：TC-08 **PASS**（http=409）、TC-17 **PASS**（http=409）、TC-63 **PASS**（winTime=`2026-09-17T07:12:32.462764+00:00`，UTC=true）；脚本汇总 **PASS=7 / FAIL=1**（唯一 FAIL 为 TC-53，已知不可达分支 OBS-03，非缺陷）。证据：`tests/e2e/logs/api-round2-regression-20260917-151229.log` |
| R2 | 回归 TC-30（BUG-03）：E2E 错误态文案实测 | **已完成（2026-09-17 15:12，以证据文件名时间戳为准）**：**PASS**，实测文案 `奖池加载失败 \| 系统繁忙，请稍后重试 \| 重试`（1.1s）；证据 `tests/e2e/logs/tc30-regression-20260917-151255.log`（**勘误**：原文误引 `…-151508.log`，该文件名在磁盘不存在）。断言已按 50 原文收紧为 `toContain('奖池加载失败')` |
| R3 | 全量 E2E 重跑（`src/` 变更 → 矩阵强制） | **已完成（2026-09-17 15:13–15:17）**：**44 passed / 6 failed**（4.1m）。6 个失败 = TC-02、TC-04、TC-07、TC-29、TC-42、TC-66；其中 TC-02 / TC-04 / TC-07 的实测响应为 **429 限流**（页面文案「系统繁忙，请稍后重试」，即注册 10 次/分钟（按 IP）窗口被前序用例占满），TC-29 / TC-42 / TC-66 为 `Test timeout of 30000ms exceeded`。**隔离复跑（`--grep`，2026-09-17 15:19）：6 passed（16.9s）**→ 判定为**用例隔离缺陷（同类 OBS-07），非产品缺陷、非 CHG-14 回归劣化**。证据：`tests/e2e/logs/full-run-regression-20260917-151311.log` + `tests/e2e/logs/isolate-rerun-20260917-151932.log` |
| R4 | 覆盖补做 13 条：TC-23、TC-25、TC-38b、TC-48c、TC-56b、TC-56c、TC-76、TC-77、TC-78、TC-79、TC-79b、TC-84、TC-85 | **已完成 7 / 13**（2026-09-17，TC-76 收官 15:52）：TC-23 / TC-25 / TC-38b / TC-48c / TC-76 / TC-77 / TC-85 已执行，脚本化于 `tests/e2e/coverage.spec.ts`（10978 → 32336 字节）。结果：TC-23 / TC-25 / TC-38b **通过**；TC-48c 通过但暴露 **BUG-04**；**TC-77 / TC-85 通过**；**TC-76 经用户裁决后 4/4 子项全绿**——①生产基线取值齐 D-06 配置键表（`Enabled=false` / `ForcedResults[]` 空 / `WeightOverrides{}` 空）、②生产配置正常启动（401 就绪探针）、③三类确定性注入全部 fail-fast、④第三条款按 **50 v3** 取值口径修订断言后复跑通过（**3 passed / 15.0s**）。原「不含该节」口径冲突 = **QX-E-07：已裁决（用户决断）并闭环**（判 TC-76 原用词过严 → 改用例；**未立 BUG**、无工程侧改动）。证据：`tests/e2e/logs/deploy-tc76-77-85-v3-20260917-155208-01.log`、`coverage-tc76-prod-start-20260917-155210-01.log`、`coverage-tc76-failfast-1/2/3-20260917-1552*.log`、`coverage-tc77-testing-20260917-155215-05.log`（首跑 15:43 同结论日志亦在目录中）；详见 `docs/52-qa-report.md` §1.6.3 / §1.6.4。**剩余 6 条**（TC-56b / TC-56c / TC-78 / TC-79 / TC-79b / TC-84）待后续实例（其中 TC-78 / TC-79 / TC-79b / TC-84 已由 **R5** 补做完毕，见下行） |
| R5 | 覆盖补做（统计 / 性能）4 条：TC-78、TC-79、TC-79b、TC-84 | **已完成（2026-09-17 16:42–16:48）；4/4 PASS**：TC-78（n=10000，最大偏差 **0.300pp** ≤ 2pp，频率合计 **100.000%**）、TC-79（n=2000，库存置 0 的 `prize-coupon` **0 次**中出，最大偏差 **1.150pp**）、TC-79b（n=2000，全局固定概率假设被拒 **20.000pp** vs 候选集实时归一 **1.150pp**；`IsEnabled` 停用维度同结论，最大偏差 **1.259pp**、earbuds 0 次）、TC-84（抽奖 P95 **116.97ms** ≤ 500ms、记录列表 P95 **14.65ms** ≤ 300ms、**0 超时 / 0 失败**）。方法：另起 Testing 实例 :5199（发布产物、`ContentRoot=发布目录`、宿主日志 `Hosting environment: Testing`）、库 `luckydraw_test`、Redis db=2、`Draw__DailyLimit=30000` / `RateLimit__DrawPerMinute=100000` **仅经环境变量注入**；库存以夹具钉高（`Stock=1000000 WHERE Type<>3`）保证候选集恒定，**运行后已复位 FR-03 默认值并 flush Redis db=2**；**未改 `src/` 一行、未改任何配置文件**。**本批 0 新缺陷**；执行中的环境侧问题（ContentRoot 致 `appsettings*.json` 不加载 → 空签名密钥 → 令牌类接口 500）属**执行方式缺陷（启动方式错误）**，纠正后全绿，另登记观察项 **OBS-11**（见 `docs/52-qa-report.md` §3）。脚本：`tests/e2e/api/stats-suite.mjs` / `tests/e2e/api/perf-suite.mjs`（共用 `tests/e2e/api/stats-lib.mjs`，无新增第三方依赖）。证据：`tests/e2e/logs/stats-run-20260917-164218.log`（12873 B）、`tests/e2e/logs/perf-tc84-20260917-164745.log`（6581 B）、实例日志 `tests/e2e/logs/stats-instance-20260917-164034.log`；详见 `docs/52-qa-report.md` §1.6.5 |
| **R6** | 破坏性批（依赖故障注入）2 条：**TC-56b**、**TC-56c** —— 覆盖缺口最终清零 | **已完成（2026-09-17 17:08–17:18）；2/2 PASS**，**新增缺陷 1 条：BUG-05（一般，`已闭环`）**。**TC-56b PASS**：以「另一连接对 `User` 行持 X 锁」注入失败，在飞期经 `sys.innodb_lock_waits` 抓到等待方 trx 状态 `LOCK WAIT`、`trx_rows_modified=4`、被阻塞语句字面 = `INSERT INTO WinningRecord`（即写事务第 ⑥ 步），抽奖在 **30.6s** 后失败；**回滚一致性 5/5 通过**（UsedCount 1→1、`prize-mug` Stock 4→4、WinningRecord 1→1、DrawRequest 1→1、AuditLog(Draw) 1→1），AUTO_INCREMENT 前进量 DrawRequest +1 / WinningRecord +1 独立佐证「行确实插入过又被回滚」。**TC-56c PASS**：停 `luckydraw-redis`（TCP 6379 → ECONNREFUSED）后重放同一幂等键 → 结果与首次全等、UsedCount 1→1、无新记录、库存逐行不变；新键抽奖 code=0 且次数正常 +1；`/draw/quota`、`/records` 可用且与库内一致；实例日志命中设计内降级 warning「读取幂等结果缓存失败，退化为数据库幂等路径」「写入幂等结果缓存失败（不影响业务结果）」→ 架构 §6.1「Redis 不是抽奖主链路的强依赖」实测成立；收尾已 `docker start` 并验证注册 + 抽奖恢复。方法与复位：**未改 `src/` 一行、未改任何配置文件**；奖池以 SQL 夹具收窄（仅 `prize-mug` 入候选）后**已复位 FR-03 默认值**（实测 ✔）、Redis db2 已 flush、`innodb_lock_wait_timeout` 全程未改（终态 50）、无残留锁持有连接 / 事务（终态 0 / 0）。脚本：`tests/e2e/api/tc56b-tx-rollback.mjs`（415 行）、`tests/e2e/api/tc56c-redis-degradation.mjs`（354 行）。证据：`tests/e2e/logs/tc56b-real-20260917-170833.log`、`tests/e2e/logs/tc56b-instance-excerpt-20260917-170943.log`、`tests/e2e/logs/tc56c-run2-20260917-171700.log`、`tests/e2e/logs/tc56-cleanup-20260917-171736.log`；详见 `docs/52-qa-report.md` §1.6.6 |
| **R7** | 修复循环 b 步回归（CHG-16 / CHG-17 修复后）：**TC-48c**、**TC-56b**、**TC-56c** | **已完成（2026-09-17 18:27–18:30）；3/3 PASS、0 新缺陷**。**TC-56b PASS**：同一锁竞争注入法（`HOLD_SECONDS=300` > 重试预算 ≈100s，确保观察到「重试耗尽」而非「重试成功」），终态 **`http=200` / `code=1001`「系统繁忙，请稍后重试」/ elapsed=101037ms**（修复前 500 / 30615ms）；回滚一致性 **5/5**（UsedCount 1→1、`prize-mug` Stock 4→4、WinningRecord 1→1、DrawRequest 1→1、AuditLog(Draw) 1→1）；AUTO_INCREMENT DrawRequest +2 / WinningRecord +2（两次尝试均插入过并回滚）；实例日志 `CommandTimeout='60'`、attempt1 阻塞 50,012ms → `[WRN] …重试整个事务（第 1 次）`、attempt2 阻塞 50,005ms → `[FTL] …重试 2 次后仍遇数据库瞬时错误…`（措辞问题即 60 v5 REV-21 所指）、`[WRN] 业务异常：Code=1001`。**TC-56c PASS**：停 Redis（ECONNREFUSED）后重放结果全等（`{"itemId":5,"isWin":false,"remainingAttempts":29999}`）、UsedCount 1→1；新键 `code=0`、UsedCount 1→2、`prize-mug` Stock 50→49；`/draw/quota` = 29998 与库内一致、`/records` `totalCount=1` 与库内一致（`[OK]`）；降级 warning 各 2 行；收尾恢复 PONG。**TC-48c PASS**：弹层「恭喜获得 三等奖 · 定制马克杯」（名称不再为空）、**奖池重拉次数 = 1**（修复前 0）、重拉后快照含目标条目、无白屏。**BUG-04、BUG-05 经实跑复验 → 均置 `已闭环`**（逐条「回归轮次 + 验证方式」见 §2）；**未闭环缺陷 0 条**。断言变更（只加强，未删弱）：新增 `terminalOk`（钉死 1001 / HTTP 200）+ `budgetOk`（≥90s 证明重试确实发生）。环境偏离：`:5180` 已停（未触碰）→ TC-48c 以脚本内 `page.route` 转发 `:5197` 重跑；TC-56b 执行实例 `:5198`；`:5199`（PID 36564）经签发令牌健康检查复验通过。证据：`tests/e2e/logs/tc56b-rerun-20260917-182721.log`、`tests/e2e/logs/tc56-rerun-instance-20260917-182525.log`、`tests/e2e/logs/tc56c-rerun-20260917-183000.log`、`tests/e2e/logs/tc48c-rerun-20260917-182926.log`、`tests/e2e/logs/tc48c-instance-20260917-182750.log`；详见 `docs/52-qa-report.md` §1.6.7 |

**60-review 纳入说明**：`docs/60-review.md` v2 重跑结论为「闭环 14 / 未闭环 5 / 新增严重及以上 0」，5 项未闭环（REV-10/11/12/16/18）**全部为建议级**且已由用户裁决不纳入本轮。按角色定义的纳入规则（未闭环的**致命 / 严重**项必须纳入 51），**本轮无 60 来源的缺陷条目**（未重开该 5 项）。**v3 更新（2026-09-17）**：§B 经独立磁盘核验确认 CHG-14 三项修复正确（与本台账 `已闭环` 判定互相印证）；新增 **REV-20**（建议级：Infrastructure 项目 `dotnet format whitespace --verify-no-changes` 为红）系**代码格式门禁发现、非测试发现的产品缺陷** → **不登记为 BUG、不进本台账**，已如实记入 `docs/52-qa-report.md` §3 **OBS-10**（测试侧独立复跑：总数 45、行范围 95–121），其是否阻断由主对话裁决。**v4 更新（2026-09-17）**：REV-20 经 CHG-15 修复后定向复审判 **已闭环**（格式门禁 45 → **0**、exit 2 → **exit 0**）；本角色同日独立复跑四个项目 `dotnet format whitespace --verify-no-changes` **全部 exit 0**（证据 `tests/e2e/logs/format-gate-rerun-20260917.log`）；v4 轮**新增发现 0 项**、其余 **19 项维持 v3 判定**、**0 致命 / 严重未闭环** → 本台账**无对应缺陷条目新增或变更**。

---

## 1. 缺陷清单

| BUG | 严重程度 | 状态 | 关联 TC | 关联 CHG / REV | 标题 | 证据 |
| --- | --- | --- | --- | --- | --- | --- |
| BUG-01 | **一般** | **`已闭环`** | TC-08、TC-17 | CHG-14 | 注册 / 登录「同键异体」返回 HTTP 200 + body `code=409`，契约要求 **HTTP 409** | 首轮：`tests/e2e/logs/api-round2-20260917-145013.log`；**回归（第 2 轮）：`tests/e2e/logs/api-round2-regression-20260917-151229.log`** |
| BUG-02 | 建议 | **`已闭环`** | TC-63 | CHG-14 | `/api/v1/records` 的 `winTime` 未携带时区标识，不符合「ISO 8601（UTC）」的完整口径 | 首轮：`api-round2-20260917-145013.log`；**回归（第 2 轮）：`api-round2-regression-20260917-151229.log`** |
| BUG-03 | 建议 | **`已闭环`** | TC-30 | CHG-14 | 奖池加载失败态标题为「加载失败」，原型冻结面为「奖池加载失败」 | 首轮：`full-run-20260917-144348.log`；**回归（第 2 轮）：`tests/e2e/logs/tc30-regression-20260917-151255.log`** |
| **BUG-04** | 建议 | **`已闭环`**（R7 复跑验证） | TC-48c | **无**（覆盖补做暴露，非 CHG 引入） | 抽奖结果条目不在当前奖池快照时：未按 50 原文「重拉奖池一次再定位」，结果弹层**奖品名称为空**（「恭喜获得」后无名称） | `tests/e2e/logs/coverage-20260917-152011.log` 中 `TC-48c :: …弹层文案="恭喜获得  已记入我的中奖记录 …"；抽奖后奖池重拉次数=0` |

| **BUG-05** | **一般** | **`已闭环`**（R7 复跑验证） | TC-56b（用例 **PASS**，缺陷系执行中暴露） | **无**（非 CHG 引入；`MySqlErrors` 分类缺失自首轮交付即存在） | 抽奖事务**后半段写入点**（⑥ 中奖记录 / ⑦ 审计 / ⑧ 回填）未做瞬时错误分类，`DrawService` 的整事务重试机制对其失效 → 锁竞争下用户得到 **HTTP 500 / `code=500`「系统内部错误」**（设计为 **`1001`「系统繁忙，请稍后重试」**），且白等 30s | `tests/e2e/logs/tc56b-instance-excerpt-20260917-170943.log`（`CommandTimeout='30'` → 30039.97ms → 500）+ `tests/e2e/logs/tc56b-real-20260917-170833.log` |

**最高严重程度：一般（BUG-05，`已闭环`）。无致命 / 严重级缺陷；未闭环缺陷 0 条。** 审查项层面未闭环 **0** 条（`docs/60-review.md` **v6**：现行 21 项 = 已闭环 16 / 已接受（用户决断）5 / **未闭环 0**；**REV-21** 经 CHG-18 修复 → `已闭环`，非本台账 BUG，见 `docs/52-qa-report.md` §2「60 审查项状态」）。 **（2026-09-20 终批后半段追记）**现行 `docs/60-review.md` = **v8**：现行 **24 项 = 已闭环 19 / `已接受（用户决断）` 5 / 未闭环 0**（`REV-22` / `REV-23` / `REV-24` 与 `60:OBS-34` 经用户 2026-09-20 裁决闭环）；**致命 / 严重未闭环 0 项**、**60 侧阻断型 `60:OBS` = 0**。本行上文「v6：21 项 = 16 / 5 / 0」为该时点历史快照，按 13.4 不回改；**审查项层面未闭环 0 条（现行）**。

> **本轮新增缺陷（1 条，来自授权覆盖补做的 TC-48c，**非** CHG-14 引入）**：见 §1.1 的 BUG-04；该缺陷为本轮把首轮「未执行」的 TC-48c 脚本化后暴露，与 CHG-14 的三项修复无因果关系。

> **本批（R5：TC-78 / TC-79 / TC-79b / TC-84）新增缺陷：0 条（显式声明）**。四类新覆盖面（统计抽样两层、概率实时归一、接口分位值）实测均满足契约阈值且未放宽断言；执行过程中的环境侧问题经独立排查确认**属执行方式缺陷（启动方式错误：发布产物以仓库根目录为 ContentRoot 启动 → `appsettings*.json` 不加载 → 空签名密钥）**，纠正后全绿，**非产品缺陷、未立 BUG**；其中暴露的「空签名密钥三处口径不一致」按要求登记为 **52 §3 OBS-11（观察项；属 REV-10 范围、用户已裁决「不纳入本轮」→ 不构成修复要求、不作为阻断项）**。

> **本批（R6：TC-56b / TC-56c 破坏性批）新增缺陷：1 条（显式声明）** —— **BUG-05（一般，`已闭环`）**，详见 §1.1。**两条用例本身均判 PASS**：TC-56b 的回滚一致性判据 **5/5 全部通过**（用例要验的「三方一致」成立），BUG-05 是执行过程中**暴露**的独立产品问题（错误码分层与重试机制失效），**不是** TC-56b 的判据失败，也不改变其结论。**TC-56c 0 缺陷**：架构 §6.1「Redis 不是抽奖主链路的强依赖」的 4 项承诺（抽奖与扣减走 MySQL / 次数与记录查询不依赖 Redis / 幂等重放退化为 DB 路径且不重复扣次 / 重放结果一致）**逐项实测成立**，并取得设计内降级 warning 的正向证据。

> **回归轮小结（2026-09-17 第 2 轮）**：BUG-01 / BUG-02 / BUG-03 三条**全部实测转绿，状态置 `已闭环`**。
> | BUG | 关联 TC | 回归结论 | 回归轮次 + 验证方式 | 证据 |
> | --- | --- | --- | --- | --- |
> | BUG-01 | TC-08、TC-17 | **通过**：注册与登录「同键异体」均已返回 **HTTP 409**（`http=409 code=409`），未签发新凭证、未产生第二次副作用 | 第 2 轮（CHG-14 后）/ 接口级 `node tests/e2e/api/api-round2.mjs` 复跑 | `tests/e2e/logs/api-round2-regression-20260917-151229.log`（TC-08 / TC-17 行） |
> | BUG-02 | TC-63 | **通过**：`winTime = 2026-09-17T07:12:32.462764+00:00`（ISO 8601=true，**UTC=true**），与库中 UTC 值 `2026-09-17 07:12:32.462764` 一致 | 第 2 轮（CHG-14 后）/ 同脚本 | 同日志 TC-63 行 |
> | BUG-03 | TC-30 | **通过（文案已改）**：错误态实测文案 = **「奖池加载失败 \| 系统繁忙，请稍后重试 \| 重试」**，与原型冻结面逐字一致；重试后进入 `page-draw--default`，次数保持 3（只读） | 第 2 轮（CHG-14 后）/ E2E `--grep "TC-30"`；**断言已按 50 原文收紧**为 `toContain('奖池加载失败')`（原为 `toContain('加载失败')`，无法区分修复） | `tests/e2e/logs/tc30-regression-20260917-151255.log` |
>
> **回归未引入新缺陷**（该批脚本汇总 `PASS=7 / FAIL=1`，唯一 FAIL 为 TC-53 的已知不可达分支 OBS-03，首轮即如此，非回归劣化）。

---

## 2. 缺陷详情

### BUG-01　（一般，`已闭环`）注册 / 登录「同键异体」返回 HTTP 200 + body `code=409`

- **编号**：BUG-01
- **关联 TC**：TC-08（步骤 3）、TC-17（步骤 3）
- **关联 CHG / REV**：CHG（`docs/30-architecture.md` **D-15 / §5.4** 注册 / 登录幂等实现）
- **严重程度**：一般
- **状态**：**`已闭环`**（第 3 轮回归 · R7 复跑验证，验证方式见本 BUG 文末「回归验证」块）
- **复现步骤**：
  1. 注册用户 `A`（`POST /api/v1/auth/register`，请求头 `Idempotency-Key: K1`，请求体 `{userName:"A", password:"Abcd1234", confirmPassword:"Abcd1234"}`）→ 200 / `code=0`。
  2. 以**同一键 K1**、**不同请求体**再次调用注册接口（`userName` 改为 `Ax`）。
  3. 观察 HTTP 状态码与响应体。
  - 登录侧同构：`POST /api/v1/auth/login`，同一键 K2 先以正确密码提交、再以 `password: "Abcd1234z"` 提交。
- **实际结果**：
  - 注册：`HTTP 200`，响应体 `{"code":409,...}`；未创建新用户（`User` 表无 `Ax` 行），未产生第二次业务副作用 —— 冲突**被识别**了。
  - 登录：`HTTP 200`，响应体 `code=409`。
  - 即：**业务码正确（409）、HTTP 状态码错误（200）**。
- **期望结果**（依据 `docs/30-architecture.md` §5.1「错误码分工」行与 §5.4）：
  - 「`409` 幂等冲突」属 §5.1 中与 `400/401/403/429/500` 并列的 **HTTP 状态码**分工，应为 **HTTP 409**；
  - `docs/50-testcases.md` §0 统一口径表亦明确「幂等冲突 → HTTP 409（30 §5.4）」，并注明「期望结果一律按此写，不得写成『返回 400』」。
  - 附带口径问题：`code=409` **低于 1000**，与 §5.1「业务异常 `code ≥ 1000`」的成功 / 业务异常二分口径不一致（`409` 未登记在 `docs/error-codes.md` 的业务码表中）。**（2026-09-17 复核补注：`docs/60-review.md` v3 §D「低于门槛的残余观察」第 3 条已**正式关闭**本附带口径问题 —— `error-codes.md:4` 边界条款明示 `4xx/5xx` 段与 HTTP 状态码对齐，`409` 由 `30-architecture.md:598` 承接登记，属设计内，不再追办）**。
- **影响范围**：
  - 本项目前端按 `response.data.code` 判定，`utils/error.ts` 已统一映射 409 文案，**用户可见行为不受影响**（TC-71 已实测前端对 409 展示「请勿重复提交」）。
  - 影响面在**契约一致性**与**非浏览器客户端**：第三方 / 移动端按 HTTP 状态码判定时，会把一次被拒绝的重复提交当作成功；同时使 §5.1 的错误码分工表对 `409` 失效。
  - 影响接口：`POST /api/v1/auth/register`（API-01）、`POST /api/v1/auth/login`（API-02）。抽奖侧 `POST /api/v1/draw` 的 409 分支在契约下不可达，见 §3 观察项 OBS-03。
- **证据**：
  - `api-round2.mjs` 运行输出（TC-08）：
    `步骤1 code=0；步骤3（同键 K1 + 不同 userName）http=200 code=409 是否签发新凭证=no；用户表新建记录数 name=1 other=0`
  - `api-round2.mjs` 运行输出（TC-17）：
    `步骤1 code=0；步骤3（同键 K2 + 不同请求体）http=200 code=409`
  - 无 Playwright 截图（接口级用例，证据为脚本标准输出；脚本可重跑复现：`node tests/e2e/api/api-round2.mjs`）。

### BUG-02　（建议，`已闭环`）`/api/v1/records` 的 `winTime` 未携带时区标识

- **编号**：BUG-02
- **关联 TC**：TC-63
- **关联 CHG / REV**：CHG（API-08 响应数据契约）
- **严重程度**：建议
- **状态**：**`已闭环`**（第 3 轮回归 · R7 复跑验证，验证方式见本 BUG 文末「回归验证」块）
- **复现步骤**：
  1. 以任一有中奖记录的用户调用 `GET /api/v1/records?pageIndex=1&pageSize=10`。
  2. 观察 `data.items[0].winTime` 的字符串形态。
- **实际结果**：`winTime = "2026-09-17T06:37:31"` —— **无 `Z` / `+00:00` 时区标识**（同时分秒后无毫秒）。该值与库中 UTC 时间一致（`WinningRecord.CreateTime = 2026-09-17 06:37:31`，UTC）。
- **期望结果**（依据 `docs/30-architecture.md` §5.1「时间 `ISO 8601 字符串（UTC）`」与 API-08 行「`winTime` = ISO 8601（UTC）」）：应携带 UTC 时区标识（如 `2026-09-17T06:37:31Z` 或 `+00:00`），使「UTC」这一口径**自描述**，而非依赖接收方事先约定。
- **影响范围**：
  - **本项目前端不受影响**：`utils/datetime.ts` 的 `parseUtc()` 对无时区标识的字符串按 UTC 解析，`formatUtc8DateTime` 再手动 `+8h`，实测页面正确展示 `2026-09-17 14:37`（见 TC-63 运行日志）。
  - 影响面在**契约自描述性与互操作**：任何直接以 `new Date(winTime)` 解析的消费者（脚本、第三方客户端、后续新增前端模块）会按**本地时间**解释该值，在 UTC+8 环境下产生 **8 小时**偏差。
  - 影响接口：`GET /api/v1/records`（API-08）。同类结构风险：`GET /api/v1/draw/quota` 的 `resetAt` 已正确携带 `+00:00`（TC-38c 实测 `2026-09-17T16:00:00+00:00`），两处口径不一致。
- **证据**：
  - `api-round2.mjs` 运行输出（TC-63）：`记录字段=id,prizeName,winTime；时间值=2026-09-17T06:37:31（ISO 8601=true，UTC=false）；DB 原始=2026-09-17 06:37:31.783156`
  - E2E 侧 TC-63 运行日志：`接口 winTime=2026-09-17T06:37:31（无时区标识）；期望展示(UTC+8)=2026-09-17 14:37；若按本地时间误解析则为=2026-09-17 06:37；页面含期望值=true`（页面展示正确，故定级为建议）

### BUG-03　（建议，`已闭环`）奖池加载失败态标题为「加载失败」，原型冻结面为「奖池加载失败」

- **编号**：BUG-03
- **关联 TC**：TC-30
- **关联 CHG / REV**：CHG（前端奖池错误态文案）
- **严重程度**：建议
- **状态**：**`已闭环`**（第 3 轮回归 · R7 复跑验证，验证方式见本 BUG 文末「回归验证」块）
- **复现步骤**：
  1. 已登录进入抽奖页。
  2. 令 `GET /api/v1/prizes` 请求失败（E2E 中以 `route.abort('failed')` 注入）。
  3. 观察 `page-draw--error` 区域的文案。
- **实际结果**：错误态文案为 **「加载失败」** + 「系统繁忙，请稍后重试」+ 重试按钮（destructive Alert）。
- **期望结果**（依据 `docs/20-prototype.html` 冻结面与 `docs/50-testcases.md` TC-30）：抽奖页奖池错误态标题应为 **「奖池加载失败」**。原型中该文案出现在状态锚点说明中：「奖池加载失败，可重试（Alert destructive）」「奖池加载失败 / 系统繁忙，请稍后重试」「奖池加载失败」+「系统繁忙，请稍后重试」+ 重试按钮。
- **影响范围**：
  - 纯文案偏差，**功能不受影响**（错误态锚点、重试按钮、重试后恢复 `page-draw--default`、重试为只读请求均已实测通过）。
  - 现实根因可理解：前端把该文案集中为 `messages.ts` 的通用键 `loadFailedTitle: '加载失败'`（这同时满足了 TC-73b 的「文案集中管理」要求），代价是丢失了「奖池」这一限定词。
  - 影响范围：抽奖页奖池错误态（`page-draw--error`）。记录页错误态（`page-records--error`）的原型口径同样是「加载失败」，无偏差。
- **证据**：
  - `tests/e2e/qa.spec.ts` TC-30 运行日志：`TC-30 :: 错误态文案="加载失败 | 系统繁忙，请稍后重试 | 重试"`
  - 前端实现：`src/frontend/src/utils/messages.ts:17` `loadFailedTitle: '加载失败'`
  - 原型冻结面：`docs/20-prototype.html`（`奖池加载失败` 出现于抽奖页错误态说明）

### BUG-04　（建议，`已闭环`）结果条目不在奖池快照时未重拉奖池，弹层奖品名称为空

- **编号**：BUG-04
- **关联 TC**：TC-48c（本轮覆盖补做，首轮未执行）
- **关联 CHG / REV**：**无**（未由 CHG-14 引入；该路径代码在首轮交付中即存在，本轮首次脚本化后暴露）
- **严重程度**：建议
- **状态**：**`已闭环`**（第 3 轮回归 · R7 复跑验证，验证方式见本 BUG 文末「回归验证」块）
- **复现步骤**（`tests/e2e/coverage.spec.ts` 的 TC-48c 已脚本化，可重跑）：
  1. 直连测试库夹具：`UPDATE PrizeItem SET IsEnabled = 0 WHERE Code = 'prize-mug'`（页面加载时该条目停用）。
  2. 注册一次性用户并进入 `/draw`：前端奖池快照**不含**该条目（实测快照 id 集合不含 `id=3`）。
  3. 抽奖前夹具：启用该条目（`IsEnabled = 1, Stock = 5`）、其余实物库存置 0、`no-prize` 权重置 0 → 该条目成为**唯一可中出候选**。
  4. 点击抽奖，观察结果弹层文案与「抽奖后是否重拉奖池」。
- **实际结果**：
  - 后端返回 `{"itemId":3,"isWin":true,...}`（即快照外条目）；前端**跳过旋转直接展示结果弹层**（不卡动画、不白屏）—— 关键不变量满足。
  - **弹层文案 = 「恭喜获得  已记入我的中奖记录  今日剩余次数：2 次  查看我的中奖记录 继续抽奖」** —— **奖品名称缺失**（`prizeName` 取自快照，快照无该条目）。
  - **抽奖后奖池重拉次数 = 0**（前端未发起第二次 `GET /api/v1/prizes`）。
- **期望结果**（依据 `docs/50-testcases.md` TC-48c 原文，FR-05-R10）：条目不在当前奖池快照时**重拉奖池一次再定位**；仍不可定位时才跳过旋转直接展示结果 —— 且「保证结果反馈不被渲染边界阻塞、不白屏」。
- **影响范围**：
  - 触发条件为**快照竞态**（页面加载后奖池发生启用/停用变更，且该条目恰好成为抽奖结果），现实窗口小 → 定级**建议**。
  - 用户可见后果：极少数情况下结果弹层出现「恭喜获得」+ 空名称，缺少奖品名（未中奖路径不受影响）。
  - 影响面：抽奖页结果弹层（`page-draw--win` 的奖品名）；「跳过旋转直接展示结果」与「不白屏」两个不变量**均已满足**。
  - 备注：`DrawResponseDto` 仅含 `itemId / isWin / remainingAttempts`（无条目名称），故「重拉奖池」是前端取得名称的唯一通道 —— 实现该步骤即可同时消除本缺陷。
- **证据**：
  - `tests/e2e/logs/coverage-20260917-152011.log`：`TC-48c :: 后端返回 itemId=3（快照外条目 id=3）；弹层文案="恭喜获得  已记入我的中奖记录  今日剩余次数：2 次  查看我的中奖记录 继续抽奖 Close"；抽奖后奖池重拉次数=0（50 原文期望「重拉奖池一次再定位」）`
  - 实现侧：`src/frontend/src/views/draw/DrawView.vue:85-89`（`resultIndex < 0` 时直接 `settled = true; dialogOpen = true`，无重拉）；`src/frontend/src/views/draw/DrawView.vue:55`（`prizeName` 仅从 `prizeItems` 快照取名，未命中返回空串）

- **回归验证（第 3 轮回归 · R7，2026-09-17 18:29）**：**复跑验证通过 → 状态置 `已闭环`**（CHG-16 前端修复后，按 TC-48c 原步骤回写复跑）。
  - 验证方式：`node node_modules/@playwright/test/cli.js test tc48c-regression.spec.ts`（本轮新增回归脚本；脚本内以 `page.route` 把 `/api/v1/**` 转发到修复后实例，环境偏离说明见 `docs/52-qa-report.md` §1.6.7）
  - 关键实测值：`1 passed (7.6s)`；抽奖弹层文案 = 「恭喜获得 **三等奖 · 定制马克杯** 已记入我的中奖记录 今日剩余次数：2 次 …」（奖品名称不再为空）；**抽奖后奖池重拉次数 = 1**（修复前实测 = 0）；重拉后快照含目标条目 = true；无白屏
  - 证据：`tests/e2e/logs/tc48c-rerun-20260917-182926.log`

### BUG-05　（一般，`已闭环`）抽奖事务后半段写入点的瞬时错误未分类：重试机制对其失效，用户得到 500「系统内部错误」而非 1001「系统繁忙」

- **编号**：BUG-05
- **关联 TC**：**TC-56b**（该用例 **PASS**；本缺陷系**执行过程中暴露**，非用例判据失败——用例判据「三方一致回滚」实测 5/5 通过）
- **关联 CHG / REV**：**无**（非 CHG-14/15 引入；`MySqlErrors` 的分类覆盖面自首轮交付即如此，本轮首次以真实锁竞争将其暴露）
- **严重程度**：**一般**
- **状态**：**`已闭环`**（第 3 轮回归 · R7 复跑验证，验证方式见本 BUG 文末「回归验证」块）
- **复现步骤**（已脚本化，可重跑）：
  1. `node tests/e2e/api/tc56b-tx-rollback.mjs`（脚本内 `TC56B_HOLD=300` 为默认；脚本会自建前置：注册用户 → 候选集收窄至 `prize-mug` → 探针抽奖建立 `UserDrawQuota` 行 → 在另一连接对 `User` 行 `START TRANSACTION; SELECT … FOR UPDATE` 持 X 锁 → 触发抽奖）。
  2. 观察抽奖请求的 HTTP 状态码、响应体与耗时。
- **实际结果**：
  - 客户端在第 **30615 ms** 收到 **`HTTP 500`**，响应体 `{"code":500,"message":"系统内部错误","data":null}`。
  - 实例日志同源证据（`tests/e2e/logs/tc56b-instance-excerpt-20260917-170943.log`）：
    ```
    [17:09:06 ERR] Failed executing DbCommand (30,016ms) [Parameters=[…], CommandType='Text', CommandTimeout='30']
    INSERT INTO `WinningRecord` (`CreateTime`, `PrizeItemId`, `PrizeName`, `UpdateTime`, `UserId`)
    VALUES (@p0, @p1, @p2, @p3, @p4); SELECT `Id`, `IsDeleted` FROM `WinningRecord` WHERE ROW_COUNT() = 1 AND `Id` = LAST_INSERT_ID();
    [17:09:06 ERR] An exception occurred in the database while saving changes for context type 'LuckyDraw.Infrastructure.Data.AppDbContext'.
    Microsoft.EntityFrameworkCore.DbUpdateException: An error occurred while saving the entity changes. See the inner exception for details.
     ---> MySqlConnector.MySqlException (0x80004005): The Command Timeout expired before the operation completed.
     ---> MySqlConnector.MySqlException (0x80004005): Query execution was interrupted
       at LuckyDraw.Infrastructure.Repositories.WinningRecordRepository.AddAsync(…) line 25
       at LuckyDraw.Application.Services.DrawService.ExecuteDrawAsync(…) line 185
       at LuckyDraw.Application.Services.DrawService.ExecuteDrawAsync(…) line 237
    [17:09:06 ERR] HTTP POST /api/v1/draw responded 500 in 30039.9728 ms
    ```
  - **无重试发生**（本角色独立复核：该窗口内 `重试` / `瞬时错误` / `retry` **命中 0 行**；4 条 ERR **时间戳全为 `17:09:06`**，系同一事件在 MySqlConnector / EF Core / 全局 ExceptionFilter 三层的重复记录，而非三次尝试）——即 `DrawService.DrawAsync` 的「最多重试 3 次整个事务」**一次都没有触发**。
- **期望结果**（依据）：
  - `docs/30-architecture.md:544`（D-08「异常与重试」）：「死锁 1213 → **整个事务重试 1 次**，仍失败 → 500 + CRITICAL 告警」；`TransientDataException.cs:4` 与 `DrawService.cs:109` 亦明文点名处理「死锁 1213 / **锁等待超时 1205**」。
  - `src/backend/src/LuckyDraw.Application/Common/ErrorCodes.cs:10` 与 `docs/error-codes.md`：**`1001`「系统繁忙，请稍后重试」**即「限流 / 依赖不可用的降级提示」，且 `error-codes.md` 明示 **500 为全局 ExceptionFilter 兜底、业务代码禁止手动使用**。
  - → 锁竞争类瞬时失败应走 `TransientDataException` 重试链，耗尽后返回 **`1001`**（HTTP 200），而非未分类的 **`500`**。
- **影响范围**：
  - **错误码分层被绕过**：客户端拿到的是「全局兜底」的 `500`，而非设计好的 `1001`；且**白等 30 秒**（`CommandTimeout=30`）才得到失败。
  - **重试机制名存实亡（结构性）**：`MySqlErrors.IsTransient`（`src/backend/src/LuckyDraw.Infrastructure/Data/MySqlErrors.cs:33`）只认 **1205 / 1213**，而抽奖事务六个写入点中**后三处完全没有分类**（实测 `catch` 计数：① `DrawRequestRepository` = 3、② `UserDrawQuotaRepository` = 2、③ `PrizeRepository` = 1、**⑥ `WinningRecordRepository` = 0、⑦ `AuditService` = 0**、⑧ `DrawRequestRepository.SaveResultAsync` 走 `ExecuteUpdateAsync` 且无 catch）→ 落在 ⑥⑦⑧ 的死锁 / 锁等待**一律直接冒泡成 500**，重试链完全跳过。
  - **「补 catch 也不够」（第二层根因）**：MySqlConnector 默认 `CommandTimeout=30`（日志实测 `CommandTimeout='30'`）**先于** MySQL 的 `innodb_lock_wait_timeout=50`（本机实测终态 50）触发**命令超时**，其 `MySqlException.Number` **不是 1205/1213** → 即便在 ⑥⑦⑧ 补上 `IsTransient` 判定，`IsTransient` 仍返回 false。**30 < 50 ⇒ 「锁等待超时 1205」这条分支在任何超过 30 秒的锁等待上都不可达**，恰好是它被写出来要处理的那个场景；修复须同时调整命令超时或把命令超时纳入瞬时判定。
  - **数据完整性不受影响**：事务仍整体回滚（TC-56b 实测 5/5 一致），不产生部分成功状态；前端对 `500` 与 `1001` 均落 `page-draw--drawfail` 且重试沿用同一幂等键（AC-17），用户可自行恢复。
  - 触发条件为**行锁竞争 / 长事务**（并发同奖品、同用户、外键父行竞争等）——正常低并发下不易触发，故**定级「一般」而非「严重」**：契约偏差明确且机制层面失效，但不损坏数据、用户可见面有限、可自行重试恢复。
- **证据**：
  - 客户端侧：`tests/e2e/logs/tc56b-real-20260917-170833.log`（`抽奖响应 → http=500 code=500 msg=系统内部错误 elapsed=30615ms`；`原始响应体 = {"code":500,"message":"系统内部错误","data":null}`）
  - 服务端侧：`tests/e2e/logs/tc56b-instance-excerpt-20260917-170943.log`（源 `tests/e2e/logs/stats-instance-20260917-164034.log` 行 34618–34725，GBK 解码后含上述三段 ERR 与 `HTTP POST /api/v1/draw responded 500 in 30039.9728 ms`）
  - 在飞期独立旁证（同一次请求）：`sys.innodb_lock_waits` = `43751 | 43749 | luckydraw_test.User | PRIMARY | … | INSERT INTO WinningRecord (…`，等待方 `trx_state=LOCK WAIT`、`trx_rows_modified=4` → 阻塞发生在 **⑥ 中奖记录 INSERT**（② 扣次 / ③ 扣库存已写入之后），非「第一条语句就失败」。
  - 代码侧（只读核对，未改动）：`MySqlErrors.cs:33`、`WinningRecordRepository.cs:25`、`AuditService.cs`、`DrawRequestRepository.SaveResultAsync`、`DrawService.cs:117-130 / 185 / 237`。

- **回归验证（第 3 轮回归 · R7，2026-09-17 18:27–18:30）**：**复跑验证通过 → 状态置 `已闭环`**（CHG-16 + CHG-17 修复后，按 TC-56b 原步骤回写复跑）。
  - 验证方式：`STATS_BASE=http://127.0.0.1:5198/api/v1 node tests/e2e/api/tc56b-tx-rollback.mjs`（同一锁竞争注入法：另一连接对 `User` 行持 X 锁；锁持有时长 `HOLD_SECONDS=300` > 重试预算 ≈100s，确保观察到的终态是「重试耗尽」而非「重试成功」）
  - 关键实测值：终态 **`http=200` / `code=1001` / msg「系统繁忙，请稍后重试」/ elapsed=101037ms**（修复前：`http=500` / `code=500` / 30615ms）；回滚一致性 **5/5**（UsedCount 1→1、`prize-mug` Stock 4→4、WinningRecord 1→1、DrawRequest 1→1、AuditLog(Draw) 1→1）；AUTO_INCREMENT DrawRequest +2 / WinningRecord +2（两次尝试的插入均被回滚的独立佐证）；实例日志：`CommandTimeout='60'`、attempt1 阻塞 50,012ms → `[WRN] 抽奖事务遇数据库瞬时错误，重试整个事务（第 1 次）`、attempt2 阻塞 50,005ms → `[FTL] 抽奖事务重试 2 次后仍遇数据库瞬时错误（死锁 / 锁等待超时），已放弃本次抽奖`、`[WRN] 业务异常：Code=1001 Path=/api/v1/draw` —— 两次尝试合计 ≈100s，与 CHG-17「MaxTransactionAttempts=2」口径吻合（也独立佐证运行的是修复后的二进制）
  - 断言变更说明（只加强、未削弱；改前/改后逐条见 `docs/52-qa-report.md` §1.6.7）：原脚本仅断言「非 200 / 非 0」即算命中缺陷，新脚本增加 `terminalOk`（终态必须恰为 HTTP 200 / code=1001）+ `budgetOk`（耗时 ≥90s，证明重试确实发生过）
  - 证据：`tests/e2e/logs/tc56b-rerun-20260917-182721.log`、`tests/e2e/logs/tc56-rerun-instance-20260917-182525.log`（FTL 行见该日志第 155 行）

---

## 3. 观察项（非缺陷，供主对话参考）

> **编号勘误（2026-09-17 追加；命名空间规则见 `docs/artifacts.md` §3.1）**
>
> 本文件（`51:`）与 `docs/52-qa-report.md`（`52:`）的 OBS 编号**原按共享序列使用，实测在 08–10 段发生分叉**：
>
> | 编号 | `51-defects.md` | `52-qa-report.md` |
> | --- | --- | --- |
> | OBS-08 | Playwright `outputDir` 每次运行前清空 | **TC-60 用例定位符缺陷（另一件事）** |
> | OBS-09 | **无此条** | Playwright `outputDir` 每次运行前清空（与 51 的 OBS-08 **为同一事实**） |
> | OBS-10 | **无此条** | REV-20 格式门禁（代码审查发现，按纳入口径**不进本台账**） |
>
> **后果**：裸写 `OBS-08` 会指错 —— 同一编号在两份产物中指向**不同内容**。
> **处置**：**不重编号**（本文件与 52 已有多处交叉引用，重编号的破坏面大于收益）；**跨产物引用一律带文件名前缀**（`51:OBS-08` / `52:OBS-08`），历史编号如实保留。本文件序列缺 09 / 10 属**命名空间独立后的正常现象，不是断号缺陷**。

| 编号 | 内容 | 判定依据 | 关联 TC | 阻断 / 非阻断 |
| --- | --- | --- | --- | --- |
| OBS-01 | **401 响应体为空**（`Content-Length: 0`，未经 `ApiResult` 包装，ASP.NET Core 默认行为） | 契约未规定 401 的响应体形态 → **契约空白**，按 `QX-E-01` 记入 52 观察项，不立 BUG（主对话已核实）；**阻断标注（2026-09-18）**：相关用例（TC-14 / TC-25b / TC-32）判据 = 401 状态码与「不返回业务数据」，实测均满足；响应体形态不牵动任何 TC 判定与结论 → **不影响任何结论的成立**。 | TC-14、TC-25b、TC-32 | 非阻断 |
| OBS-02 | **登出后 access token 在 2h 有效期内仍可访问受保护接口** | `docs/30-architecture.md` API-04 明确为 `[AllowAnonymous]` + 仅按 Cookie 清理 refresh 键的**既定设计**（无 access token 黑名单）；前端在 `finally` 清态跳转，用户可见行为符合 FR-02；**阻断标注（2026-09-18）**：属架构既定语义（API-04）、行为符合 FR-02，TC-18 实测通过 → **不影响任何结论的成立**。 | TC-18 | 非阻断 |
| OBS-03 | **抽奖同键异体的 409 分支在契约内不可达**。对侧同一事实：↔ 52:OBS-03 | D-03 规定抽奖请求体恒为 `{}`，其规范化哈希基于空对象常量；额外字段不参与哈希 → 「请求体不同」不可构造，服务端按幂等**正常重放**（实测 HTTP 200 / `code=0`，仅扣 1 次、流水行 1 条）。核心不变量（不产生第二次副作用）满足；**阻断标注（2026-09-18，默认档）**：**可测性缺口**——TC-53 契约分支（同键异体 → 409）在本契约下不可构造、该用例判据**未按设计达成**（§5 已记「未达断言口径」）→ 牵动「覆盖是否达标」的成立（D1 分子的「明确结论」口径）→ 拿不准按**默认档**登记；是否放宽口径须主对话 / 用户裁决（见 `docs/52-qa-report.md` §4 新增待确认项）。**【改判（2026-09-20，用户决断）：末列「阻断」→「已接受（用户决断）」】**依据 = 用户 2026-09-20 裁决（经主对话调度指令转达；随 `docs/artifacts.md` §3.1 / `docs/state-machine.md` D2 第 2 项**三态闭集**落地：`阻断` / `非阻断` / `已接受（用户决断）`，第三态**不计入 D2 阻断**）。**接受人 = 用户**（2026-09-20 逐项裁决接受，本轮不修）。**理由 = 该分支在 D-03 契约下不可构造**（属可测性缺口，非产品行为缺陷）：核心不变量（仅扣 1 次、无第二次副作用、流水行 1 条）实测满足，改契约 / 改用例的破坏面大于收益。**补丁计划 = 下一轮**由主对话调度「契约口径修订」轮：在 `docs/30-architecture.md` D-03 增补「同键异体分支不可构造」的显式声明（或钉死请求体指纹语义），并据此订正 `docs/50-testcases.md` TC-53 期望口径；落地后按 `docs/development-spec.md` 7.5 复跑 TC-53 并回写本行。**注**：本条与 `52:OBS-03` 为同一事实（事实级去重，并集只计一次）。 | TC-53 | 已接受（用户决断） |
| OBS-04 | **§2.8-4 的一次性闸门为「每页面实例」语义** | 两个独立页面实例各自持内存态，实测各自发起 1 次 refresh（合计 2 次），同实例内为 1 次。跨实例去重不在契约要求内；**阻断标注（2026-09-18）**：契约语义即「每实例一次性」，TC-22 实测通过且合计 2 次 refresh 与语义一致 → **不影响任何结论的成立**。 | TC-22 | 非阻断 |
| OBS-05 | **应用日志未落盘为文件**。对侧同一事实：↔ 52:OBS-05 | Serilog 仅 `WriteTo: Console`，无法离线检索日志内容；TC-81 的「日志不含明文密码 / token」改为以 `AuditLog` 表 + `DrawRequest.RequestHash` 为可检索面验证（均无命中），并复以 `User.PasswordHash` 为 BCrypt（TC-82）交叉印证；**阻断标注（2026-09-18，默认档）**：观察面 = **证据可检索性**（日志未落盘）→ 属**证据类**，按默认档登记；TC-81 改道 `AuditLog` / `DrawRequest.RequestHash` / `User.PasswordHash` 不能消除「判据面受限」本身。**【状态回写（2026-09-20 终批前半段，依据 code-reviewer 的独立复审）】→ `已闭环`；末列由「阻断」重标「非阻断」**。闭环依据（**独立复现**，非本角色自证）：`src/backend/src/LuckyDraw.Api/appsettings.json` 已新增 Serilog `File` sink（`logs/luckydraw-.log`、按日滚动、保留 7 份；对应改动登记 CHG-21）→ **「应用日志未落盘为文件」这一证据类事实已消除**；新增守卫用例 `tests/integration/LuckyDraw.IntegrationTests/SerilogFileSinkTests.cs`。`docs/60-review.md` v7 §J 记录：集成套件 **45 / 0 / 0**（14 s；经仓库既有的隔离覆盖变量 `LUCKDRAW_TEST_CONNECTION_STRING` 指向独立实例 `:3407`），负控 **N1–N5 全部真实捕获**（N5 = 缺失 sink 程序集时抛 `FileNotFoundException`），落盘日志由 982 B 基线增至 **200 169 B / 1360 行**，13 类凭据模式命中 **0**（仅 1 处探针标记 `OBS05LOGMARK`）。证据目录（绝对路径，留存不删）：`D:\AI test\AI-Agents-workflow-Engineer-Rules\tests\integration\_tmp-rev-obs05-20260920-01`。**重标依据（`docs/state-machine.md` D2 第 2 项）**：该证据类事实既已消除，其唯一残余（TC-81 判据面受限）属**用例面**且已由 `AuditLog` / `DrawRequest.RequestHash` / `User.PasswordHash` 三面旁证覆盖，不再构成本轮阻断事实；与 `51:OBS-12` 的「闭环后重标非阻断」先例一致。**范围声明**：本角色本轮**未重跑** TC-81（E2E 实例未就绪）；`docs/52-qa-report.md` §2 结论重核不在本轮范围。 | TC-81 | 非阻断 |
| OBS-06 | **服务端未提供时钟注入通道**。对侧同一事实：↔ 52:OBS-06 | 跨日重置与时间口径只能以「按 `DrawDate` 自然日分桶」的夹具等价验证（TC-57/58/59），23:59:59 / 00:00:00 的**逐秒边界**无法执行；**阻断标注（2026-09-18，默认档）**：属**环境 / 工具链类**（无时钟注入 → 23:59:59 / 00:00:00 逐秒边界不可构造），按默认档登记；TC-57/58/59 系夹具等价执行。**【改判（2026-09-20，用户决断）：末列「阻断」→「已接受（用户决断）」】**依据 = 用户 2026-09-20 裁决（转达渠道同 `51:OBS-03`）。**接受人 = 用户**（2026-09-20 逐项裁决接受，本轮不修）。**理由 = 服务端未提供时钟注入通道**（属环境 / 可测性类缺口）：23:59:59 / 00:00:00 逐秒边界不可构造、TC-57/58/59 系夹具等价执行，引入时钟注入属 `src/` 架构级改动。**补丁计划 = 下一轮**由主对话评估是否引入时钟注入通道（如 `TimeProvider` 抽象）；若引入则复跑 TC-57/58/59 的逐秒边界并回写本行，未引入前维持接受态。**注**：与 `52:OBS-06` 为同一事实（事实级去重）。 | TC-57、TC-58、TC-59 | 已接受（用户决断） |
| OBS-08 | **Playwright 的 `outputDir` 每次运行前会被自动清空**（Playwright 默认行为，非人工删除）。对侧同一事实：↔ 52:OBS-09 | `tests/e2e/.artifacts/` 在每次 `node node_modules/@playwright/test/cli.js test` 启动时被清空，故**上一轮（14:35–14:38）4 个失败截图 / trace 目录已被后续复跑覆盖，磁盘上不再存在**。可持久化的证据改为落在 `outputDir` 之外的 `tests/e2e/logs/*.log`（重定向全量标准输出）。当前可核验证据：`tests/e2e/logs/full-run-20260917-144348.log`（47 passed / 3 failed）+ `tests/e2e/.artifacts/` 下本轮 3 个失败目录；**阻断标注（2026-09-18，默认档）**：属**工具行为 + 证据类**（`outputDir` 每次运行前自动清空），按默认档登记；实测（2026-09-18）`tests/e2e/.artifacts/` 目录**已不存在**，本行所引「本轮 3 个失败目录」当前不可核验（证据强度按**旁证**计；见头部「引用勘误」行）。**【修复进展登记（2026-09-20，本角色）；取值维持「阻断」，本轮不标 `已闭环`】**对侧同一事实 = `52:OBS-09`。修复内容：`playwright.config.ts` 的 `outputDir` 改为可经环境变量 `PLAYWRIGHT_OUTPUT_DIR` 覆盖（未设该变量时取值与历史默认逐字一致 = `./tests/e2e/.artifacts`；无新增依赖）。验证（本角色实测，2026-09-20）：① 带变量运行 1 条 e2e 用例 → 失败留痕（`test-failed-1.png` / `trace.zip` / `error-context.md`）**全部**落在变量指定目录；② 不带变量对照运行 → 留痕落在默认目录 `tests/e2e/.artifacts`。两次运行均**因无实例在跑（`ECONNREFUSED ::1:5173`）而失败**，故该两次运行**不产生任何 TC 结论**（环境未就绪，按 `docs/role-protocol.md` §5 无效），仅用于验证 `outputDir` 取值生效。证据目录（绝对路径，留存不删）：`D:\AI test\AI-Agents-workflow-Engineer-Rules\tests\e2e\_tmp-obs09-verify-20260920-01`（含改前 / 改后 sha256、两次运行日志、原始命令）。**语义边界**：本覆盖**不改变** Playwright「每次运行前清空所选 outputDir」的语义，只允许把该目录指向**每轮独立路径**。**闭环条件（尚未满足）**：按 `51:OBS-12` 先例须经**独立复现验证**后方可置 `已闭环` 并重标「非阻断」——留待终批（届时 `51:OBS-05` 的修复亦已落地）。**【进展登记（2026-09-20 终批前半段，本角色）；取值维持「阻断」，本轮不标 `已闭环` —— 闭环待该前提落载体】**① 已落地部分：`playwright.config.ts` 的 `outputDir` 已可经环境变量 `PLAYWRIGHT_OUTPUT_DIR` 覆盖（未设该变量时取值与历史默认逐字一致）；判别轮（设变量 → 独立目录 `outputdir-env-01`，13:57）与对照轮（不设变量 → 默认目录，13:58）均已实测；**判别轮的 4 件失败留痕在对照轮运行之后仍留在磁盘上**（`outputdir-env-01/` 及其下用例名子目录内：`test-failed-1.png` 4254 字节、`trace.zip` 21069 字节、`error-context.md` 822 字节、`.last-run.json` 96 字节；本角色落盘前逐件读盘核验，文件时间 13:57 均早于对照轮 13:58、且此刻仍在）；证据目录（绝对路径，留存不删）：`D:\AI test\AI-Agents-workflow-Engineer-Rules\tests\e2e\_tmp-obs09-verify-20260920-01`。② **仍缺的程序化前提**：闭环要求「**每轮**把该变量指向一个**不同的独立目录**」，而**该规程当前无任何载体承载**（配置文件只**读**该变量、不写它；全仓**无任何地方自动设置 `PLAYWRIGHT_OUTPUT_DIR`**，本角色全仓检索核实）→ 其载体归属（落入哪份规范 / 由谁维护）**待用户裁决**，故本轮**闭环待该前提落载体**。③ 语义边界（沿前）：本覆盖**不改变** Playwright「每次运行前清空所选 `outputDir`」的语义，只允许把该目录指向**每轮独立路径**——「上一轮留痕不再被后续运行抹掉」这一闭环条件因此**仍未满足**。**【状态回写（2026-09-20 终批后半段，本角色实测）→ `已闭环`；末列由「阻断」重标「非阻断」】**（对侧同一事实 = `52:OBS-09`，同批回写）**闭环程序已落载体 + 直接证明完成**：① **前提已落载体**：`docs/role-protocol.md` §7 第 1 条 + `.claude/agents/test-executor.md` 第 6 条（每轮必须把 `PLAYWRIGHT_OUTPUT_DIR` 指向本轮独立目录；不设该程序则连跑两轮都落默认目录、上一轮产物照样被抹掉 —— 该程序是本闭环的成立条件）。② **直接证明（两轮独立目录实跑 + 跨轮读回；运行时点 `date` 实测 15:52–15:53）**：Run 1（15:52:08→15:52:09，exit=1）→ `outputdir01`；Run 2（15:53:13→15:53:14，exit=1）→ `outputdir02`；**Run 2 之后重扫 `outputdir01`：4 件产物（`test-failed-1.png` 4254 B / `trace.zip` 21091 B / `error-context.md` 822 B / `.last-run.json` 96 B）全部仍在，sha256 与 Run 1 后清单逐条一致（diff 退出码 0）** → 「上一轮留痕不再被后续运行抹掉」成立。补充读数：默认目录 `tests/e2e/.artifacts` 两轮后仍**不存在**（产物确实不落默认目录）。③ **运行前提声明**：两轮均在无实例环境（`:5173` 未启动，实测 `ECONNREFUSED ::1:5173`）下运行 → **两轮的用例结论一律无效，不记为任何 TC 结果、不记缺陷**（按 `docs/role-protocol.md` §5）；仅用于验证 `outputDir` 行为。④ **回归轮次 + 验证方式** = 2026-09-20 终批后半段（两轮独立目录实跑 + 跨轮 sha256 读回比对；证据强度 = **旁证**，非版本控制级）。⑤ **重标依据（D2 第 2 项）**：闭环程序落载体后该工具行为可被每轮独立目录吸收 → **不再影响任何结论的成立** → 标「非阻断」（与 `51:OBS-12` / `52:OBS-10` 先例一致）。证据目录（绝对路径，留存不删）：`D:\AI test\AI-Agents-workflow-Engineer-Rules\tests\e2e\_tmp-obs09-persist-20260920-01`（两轮日志、Run 1 后 / Run 2 后 sha256 清单、读回比对、证据摘要 `07-verify-summary-obs09-persist-20260920-155327.md`）。**语义边界（沿前）**：Playwright「每次运行前清空所选 `outputDir`」的语义未变；本闭环的成立条件 = 每轮指向独立目录（程序已落载体）。 | 全体 E2E | 非阻断 |
| OBS-07 | **顺序相关不稳定（用例侧问题，非产品缺陷）**。对侧同一事实：↔ 52:OBS-07 | 全量串行运行时 TC-16 / TC-37 / TC-69 / TC-70 失败（**3 项均为 `Test timeout of 30000ms exceeded`，非断言失败**），按 `--grep` 隔离复跑全部通过（主对话复核：3 passed）。共同签名：三条用例均以 `safeRegister()` 开头，即**先注册新用户**；全量串行时所有用例共用同一出口 IP，注册 / 登录限流窗口（10 次/分钟，`appsettings.json`）被前序用例占满，`safeRegister` 的 429 退避重试（每次约 11–12 s）耗尽 30 s 用例超时 → 超时失败；隔离复跑窗口空闲 → 秒级通过（实测 2.8 s / 0.9 s / 4.3 s）。**根因是用例共享 IP 限流窗口与 MySQL / Redis 状态，属用例隔离缺陷**，非产品缺陷。复跑证据见 `52-qa-report.md` §1.5 / OBS-07；**阻断标注（2026-09-18，默认档）**：属**环境 / 执行侧类**（共享出口 IP 限流窗口与 MySQL / Redis 状态）；**本行结论取自隔离复跑，全量运行不稳定**（同签名跨轮复发）→ 按 `docs/development-spec.md` 7.6 / `docs/role-protocol.md` §5.4 须作**用例缺陷**处置（补隔离手段）后方可用作回归基线，尚未完成 → 影响回归基线的成立。**【改判（2026-09-20，用户决断）：末列「阻断」→「已接受（用户决断）」】**依据 = 用户 2026-09-20 裁决（转达渠道同 `51:OBS-03`）。**接受人 = 用户**（2026-09-20 逐项裁决接受，本轮不修）。**理由 = 属用例隔离缺陷**（共享出口 IP 限流窗口 + MySQL / Redis 状态；非产品缺陷）：全量运行不稳定、同签名跨 ≥2 轮复发 → 回归基线不可重复，补隔离手段属 `tests/e2e/**` 用例面改造。**补丁计划 = 下一轮**由 engineer（用例侧）补用例级隔离（独立限流窗口 / 前置一次性建号 / 夹具复位），落地后按 `docs/development-spec.md` 7.6 重跑全量回归基线并回写本行。**注**：与 `52:OBS-07` 为同一事实（事实级去重）；对侧同项待确认（`docs/52-qa-report.md` §4 `QX-E-06`）同批已消解。 | TC-16、TC-37、TC-69、TC-70 | 已接受（用户决断） |
| OBS-11 | **空签名密钥的三处口径不一致 → 「应用可启动但令牌类接口全部 500」**（R5 执行侧发现，**属 REV-10 范围**）：同一「签名密钥为空」场景存在三种处理 —— ①校验侧 `src/backend/src/LuckyDraw.Api/Program.cs:76-78` 回退为 32 个 `'0'`；②签发侧 `src/backend/src/LuckyDraw.Infrastructure/Tokens/JwtTokenService.cs:40-42` **无回退**（空串直接进 `SymmetricSecurityKey`）；③`src/backend/src/LuckyDraw.Application/Services/AuthService.cs:283-285` 幂等指纹回退为 32 个 `'0'`（实现注释已自述属 REV-10 范围）。**实测后果**（本轮因错误启动方式真实复现）：应用**照常启动、不 fail-fast**；任何签发令牌的接口（注册 / 登录）返回 `HTTP 500` + 仅「系统内部错误」；且**失败时账号已不可收回** —— 客户端看到 500，但 `User` 行与 `AuditLog` 行已入库、Redis 刷新键已写入（磁盘证据：`luckydraw_test.User` 中 Id=387–390 = `st_fqjabbik` / `probe001` / `probe002` / `probe003`，创建于该故障窗口 08:21–08:25 UTC）。即便签发成功，校验侧（回退值）与签发侧（空串）口径也**永不可能互相匹配**。**处置声明：本条仅为观察记录**（触发条件为本轮启动方式错误、已纠正），**属 REV-10 范围，用户已裁决「不纳入本轮」→ 不构成新的修复要求、不作为阻断项、不登记为 BUG**。对侧同一事实：↔ 52:OBS-11 | **执行侧**（用户已裁决「不纳入本轮」，属 REV-10 范围）；**阻断标注（2026-09-18，默认档）**：本行原「不作为阻断项」系散文式否定（`docs/artifacts.md` §3.1 与 D2 第 2 项明文禁止的形态）→ 改结构化取值；该事实含**产品级行为**（应用可启动、令牌类接口全部 500、失败时账号不可收回）且**未修复** → 拿不准按**默认档**登记；其与既有用户裁决的关系（D2 无接受路径）列入 `docs/52-qa-report.md` §4 新增待确认项。**【改判（2026-09-20，用户决断）：末列「阻断」→「已接受（用户决断）」】**依据 = **用户 2026-09-20 裁决：REV-10「不纳入本轮」的裁决范围覆盖本观察项的阻断认定**（该口径原列为 `docs/52-qa-report.md` §4 `QX-E-10` 待确认，现已明示）。**接受人 = 用户**（2026-09-20 逐项裁决接受，本轮不修）。**理由 = 本条属 REV-10 范围**（空签名密钥三处口径不一致 → 令牌类接口全部 500、失败时账号不可收回）：该事实**影响结论成立**且未修复 → 不能标 `非阻断`，按闭集只能登记第三态。**补丁计划 = 随 REV-10 修复轮一并处置**（三处口径统一 + 启动期 fail-fast）；修复落地后由本角色独立复现「空签名密钥 → 启动即失败、令牌类接口不再 500」并回写本行与 `52:OBS-11`。**注**：与 `52:OBS-11` 为同一事实（事实级去重）；本条**不计入 D2 阻断**（D2 第 2 项明文：第三态不属阻断）。 | —（执行环境发现，非用例发现；根因排查与纠正记录见 `52-qa-report.md` §1.6.5「执行方式说明」） | 已接受（用户决断） |
| OBS-12 | **【状态：`已闭环`（2026-09-20 Wave D，本角色独立复现验证）】** **集成测试夹具的 Redis 覆盖值是「半截」的**：`LuckyDrawApiFactory.cs` 声明 `LUCKDRAW_TEST_REDIS` 可覆盖 Redis 目标（默认 `localhost:6379,defaultDatabase=1`）并已注入被测应用（`Redis__Configuration`），但 `IntegrationFixture.cs` **三处硬编码 `localhost:6379`**（连接 / `ResetAsync` 的 `FlushDatabase(1)` / `ResetIdempotencyCacheAsync` 的 `Keys(database: 1)`）→ 夹具**绕过同袍文件声明的覆盖值**。**运行期实测**（2026-09-20，隔离 Redis 三库预置 marker；`LUCKDRAW_TEST_REDIS="localhost:6379,defaultDatabase=2"` 跑全量集成，两轮全部留存原始日志）：**db1 marker 被抹（EXISTS 1→0 = 命中硬编码）/ db0、db2 marker 未被触碰（1→1）；应用实际落 db2 且新增 71 键未被任何清理**（39 个 `draw:auth:refresh:*` + 2 个 loginfail + 30 个 `draw:idempotency:*`；db2 探针总键数 72 = 71 应用键 + 本实验 marker）；全部覆盖值路径运行（两轮全量 + 隔离单例）结束后 db2 累计 **143** 键；同批全量失败 1 例（`AuthApiTests.Refresh_WithoutCookie_Reports1204`：实得 1203 vs 期望 1204，2/2 轮复发）。**对照实验**：无覆盖值全量 **43/0/0**；该用例单独跑（带 / 不带覆盖值）**均通过** → 按 `docs/development-spec.md` 7.6 顺序判定 = **用例隔离缺陷**（夹具清理错靶 → 应用所用 db 跨用例残留 → 陈旧会话使该用例走 1203 分支），**非产品缺陷**（产品代码未动、不立 BUG）。**未实测场景（旁证）**：若 6379 无监听，夹具连接失败将发生在任何用例进入测试体**之前**（43 例全部不可运行）——隔离容器禁停（环境约束），未实测，按代码路径推导并列入复核点。 | 归类（`docs/artifacts.md` §3.1 闭集）：**执行侧**（测试基建 / 夹具；修复面在 `tests/integration/**`，非 `src/`）。**为何不立 BUG**：不属产品行为缺陷 —— 现象由测试夹具造成、产品行为不受影响（对照实验全量 43/0/0 且该用例隔离复跑通过），属执行侧 / 工具链类问题；但须按 D2 门禁口径承接其后果，故立观察项而非遗失。**阻断标注（2026-09-20，默认档）**：属**环境 / 证据 / 工具链类**（夹具清理错靶；且文件声明支持覆盖、实现不生效，属证据生成链完整性）→ 按默认档登记为**阻断**。**不满足「不影响任何结论的成立」豁免条件**：任何按该文件自身声明使用覆盖值的运行（如 CI 指向自有 Redis）都会产生 ①对**非目标库**的静默 `FlushDatabase(1)` **破坏**（实测抹除既有 key）、②应用实际所用库**从未被清理**（跨用例隔离失效，已实测引发确定性用例失败）——结论可在该路径上被静默污染。**责任人**：engineer（`tests/integration/**` 不在本角色写入范围）；**计划复核时点**：主对话调度的修复轮完成后的**下一轮集成回归**（由本角色按本行复现步骤复跑验证，含补测「6379 无监听」场景）。 **【2026-09-20 Wave D 闭环回写（本角色实测）】**：状态 → `已闭环`；末列取值由「阻断」重标 **非阻断** —— 判别口径 = `52:OBS-10` 先例（修复经独立复现并闭环 → **不再影响任何结论的成立**）。独立复现读数（2026-09-20 Wave D，全部为本角色自跑；原始日志 / 探针输出 / 快照存证据目录）：① **判别轮**（`LUCKDRAW_TEST_REDIS="localhost:6379,defaultDatabase=2"`，覆盖值路径）实测 `Failed: 0, Passed: 43, Duration: 13 s`（exit=0）；探针 = db2 marker 被抹（EXISTS 1→0）、db0 / db1 存活（1→1）→ 清理靶随应用实际落库（db2）。② **对照轮**（无覆盖值，默认路径）实测 `Failed: 0, Passed: 43, Duration: 13 s`（exit=0）；探针 = db1 marker 被抹、db0 / db2 存活 → 默认行为与修复前等价、无回归。③ **守护断言非空转（阴性样本）**：临时改 `RedisDatabase` 为 `=> 1`（文件 sha256 `46a98f69…` → `5bc82b4b…`）后判别轮实测 `Failed: 43, Passed: 0, Duration: 66 ms`（exit=1），守护消息「夹具 Redis 清理目标与应用不一致：应用 = Unspecified/localhost:6379 / db2；夹具 = Unspecified/localhost:6379 / db1。」命中 43 次 → 有辨别力；随后逐字节还原并复核 sha256 与备份一致（`46a98f6966753c64ff6ed6f567ee7c54e4e6b9afad636d09e72dcbfda32c3196`），复跑 `Failed: 0, Passed: 43`。④ **死端口轮**（`localhost:6399`）实测 `Failed: 43, Passed: 0, Duration: 58 ms`（exit=1）→ 原「未实测场景②」转实测（其证据边界另登记为 `51:OBS-14`）。证据目录（绝对路径，留存不删）：`D:\AI test\AI-Agents-workflow-Engineer-Rules\tests\integration\_tmp-obs12-close-20260920-01`；证据强度 = **旁证**（`dotnet test` 原始输出 + `docker exec … redis-cli` 探针 + 会话内 sha256 实测；本批尚未提交 → 非版本控制级）。**回归轮次 + 验证方式** = 本块 ①–④（第 1 轮独立复现验证）。遗留 = 「同一端口 6379 被停」具体形态未实测（见 `51:OBS-14`）。 | —（执行侧 / 夹具发现，非用例发现；影响面 = 集成全量 43 例） | 非阻断 |
| OBS-13 | **集成用例 `AuthApiTests.Refresh_WithoutCookie_Reports1204` 的预置条件与用例名不符**（来源 = `docs/40-changelog.md` **CHG-20** 文末登记的 `QX-02`；本轮本角色独立复核维持该登记）：夹具的 `Client` 由 `WebApplicationFactory` 默认参数创建（`HandleCookies` 默认 `true`）→ 该用例发出的请求**实际携带历史 refresh cookie**，走的是「会话不存在 → `1204`」分支（`AuthService` 在会话缺失时抛 `SessionExpired`），而非用例名所指的「未携带 cookie → `1204`」分支（`AuthController` 在 cookie 为空时抛 `SessionExpired`）。**实测**（2026-09-20 Wave D 判别轮 / 对照轮两轮全量，原始日志见证据目录 04b / 08b 号文件）：该用例两轮均通过（各轮 43/43）→「`1204`」这一观测值来自会话缺失分支；「无 cookie」分支在本套件中**从未被实际构造**。（口径注：上述分支归属由代码路径读取 + `WebApplicationFactory` 的 `HandleCookies` 默认值推得；本轮**未单独加探针**实测「请求是否携带 cookie」本身，如实标注为**推论**环节。） | 归类（`docs/artifacts.md` §3.1 闭集）：**执行侧** —— 用例自身 / 夹具用法使目标分支「测不了」（改用独立 `HttpClient` 并置 `HandleCookies = false` 即可构造 → 非契约不可构造，故不属「可测性缺口」；契约亦未钉死「无 cookie 时返回哪个码」，故非「契约空白」）。**为何不立 BUG**：不属产品行为缺陷 —— 实测产品返回 `1204` 为契约登记错误码（`docs/error-codes.md`），且 `1203` / `1204` 的前端恢复语义已由 `TC-20` 覆盖；本项仅为用例面的覆盖口径问题。**为何标非阻断**：**该观察项不影响任何结论的成立** —— 无任何结论以「该用例覆盖无 cookie 分支」为条件（该分支属实现细节，契约未钉死其错误码；该用例本身仍按其实际执行的分支通过）。**责任人**：engineer（`tests/integration/**` 归属 engineer）；**计划复核时点**：主对话对 `40:QX-02` 裁决后的下一轮集成回归（修法二选一：独立 `HttpClient` 构造无 cookie 分支，或订正用例名 / 断言以匹配实际分支）。 | —（执行侧 / 用例面发现，非 50 用例发现；影响面 = 集成套件 1 例的覆盖口径） | 非阻断 |
| OBS-14 | **集成套件在结构上无法覆盖「应用在 Redis 降级下的行为」**（`docs/30-architecture.md` §6.1 依赖降级矩阵的语义面；来源 = `51:OBS-12` 原「未实测场景②」，经 CHG-20 转为实测、本轮由本角色独立复现）：夹具在初始化期即建立 Redis 连接（`IntegrationFixture.InitializeAsync()` → `EnsureRedisTargetMatchesApp()`），Redis 目标不可达时 43 例**全部在夹具初始化期失败、无任何用例进入测试体**。**实测**（2026-09-20 Wave D，死端口等价构造 `LUCKDRAW_TEST_REDIS="localhost:6399,defaultDatabase=2"`，原始日志见证据目录 19b 号文件）：`Failed: 43, Passed: 0, Skipped: 0, Total: 43, Duration: 58 ms`（exit=1）；首条异常 = `RedisConnectionException`（`It was not possible to connect to the redis server(s)`，命中 43 次）；栈落点 = `IntegrationFixture.InitializeAsync`（43 次）→ **无用例进入测试体**；该轮跑前 / 跑后三库 marker 均存活（未写入任何键）。**未实测的具体形态（如实登记）**：以「同一端口 `6379` 被停」构造的变体仍未实测（本轮环境约束禁止停 / 删隔离容器；其与死端口形态的 TCP 语义等价属推论）——**责任人**：主对话（决定是否在后续轮次调度「停容器」形态的补测）；**计划复核时点**：下一次涉及 Redis 降级语义的回归轮。 | 归类（`docs/artifacts.md` §3.1 闭集）：**执行侧** —— 夹具 / 工具链结构使该行为在本套件「测不了」（非契约使然，故非「可测性缺口」）。**为何不立 BUG**：非产品缺陷 —— ① 降级行为已在 E2E 层以真实停容器形态验证（`TC-56c`，R6 实测 2/2 PASS，证据 `tests/e2e/logs/tc56c-run2-20260917-171700.log`：停 `luckydraw-redis` 后幂等重放结果全等、主链路可用、次数 / 记录查询与库内一致）；② 本项仅界定集成套件的证据边界，未留下任何契约行为的未验证面。**为何标非阻断**：**该观察项不影响任何结论的成立** —— 契约行为由 `TC-56c` 以更强形态（真实停依赖）覆盖；无任何结论以「集成套件覆盖降级行为」为条件（全量检索 `52-qa-report.md` 对 `tests/integration` 零命中）。 | —（覆盖方 = `TC-56c`；执行侧 / 夹具结构，非用例发现） | 非阻断 |
| OBS-15 | **TC-81 判据面升级已落盘、但未在就绪环境复跑；且 5 个候选扫描面中 2 个缺位（`src/backend/src/LuckyDraw.Api/logs`、`tests/e2e/deploy/publish-tc76/logs`）**（来源 = 本批 `CHG-21` 复核的判据面处置）：① 按 `docs/40-changelog.md` CHG-21 §9 的移交，`tests/e2e/api/api-suite.mjs` 的 TC-81 已改为「落盘日志正文扫描（12 闸门模式 + 6 词面模式）+ 三面 DB 检索 + 空转防护 + 扫描器自证」（改前 / 改后 / 理由见 `docs/52-qa-report.md` §1.6.8）；**该套件本轮未复跑**（环境未就绪：`:5173` / `:3307` 无实例；按 `docs/role-protocol.md` §5，非就绪环境下的运行不产生任何 TC 结论）。② 扫描面实测（本角色干跑，2026-09-20）：存在 3 面共 **3 864 133 B**（含业务流量落盘日志 2 份），12 闸门模式命中 **0**、扫描器自证通过；缺位 2 面 = `src/backend/src/LuckyDraw.Api/logs`（当前未出现）与 `tests/e2e/deploy/publish-tc76/logs`（`tests/e2e/deploy/` 系 `.gitignore:46` 的构建产物目录、当前未发布 ⇒ 部署类用例 TC-76 / TC-77 / TC-85 如需原位复跑须重新 `dotnet publish`）。 | 归类（`docs/artifacts.md` §3.1 闭集）：**执行侧 / 证据类**（复跑与扫描面）。**为何不立 BUG**：非产品行为缺陷 —— ① 产品侧事实（凭据类模式未入落盘日志正文）已由本角色干跑实测（3.86 MB / 0 命中）覆盖，且 TC-81 的三面 DB 判据维持既有 PASS 结论；② 缺位扫描面为构建产物目录（可重建），非产品行为。**为何标非阻断**：**该观察项不影响任何结论的成立** —— 覆盖口径（98/98）与既有 TC-81 结论不以其为条件；TC-81 每次运行都会把各扫描面「存在 / 不存在」如实写进 detail（不存在时记「未扫描」，绝不假称已扫）。**责任人**：test-executor（下一轮 E2E 实例就绪时复跑 TC-81 并回写本行）；deploy 产物是否重新发布由主对话调度决定。**计划执行时点**：下一轮 E2E 回归（实例就绪）。 | TC-81 | 非阻断 |

> **`51:OBS-12` 的 D2 落位与「是否构成第 7 条阻断」问答（2026-09-20）**：① **判据 = 计价命名空间**：`docs/state-machine.md` D2 第 2 项对 test-executor 侧阻断型观察项的标注归属写作 `52:OBS-nn`；本项按缺陷台账归属登记于 `51:OBS-12`（本表，已填结构化「阻断」列）→ **未径行计入 52 §3 的「6 条」统计、即不自动构成第 7 条**。② **若主对话裁量计入**（即登记为 `52:OBS-12`）：则构成 52 现有 6 条之外的第 7 条，且 52 §2 结论行 / 判定行内「阻断 `52:OBS` 共 6 条」字样须同步重核 —— 该处受本轮调度约束保护（明文不得改动），**本角色未自行登记进 52，驻留主对话裁决**。③ **并行事实**：`docs/60-review.md` 现行台账含阻断型 `60:OBS-32`（未闭环；D2 第 2 项字面含 `60:OBS-nn`），该条**亦从未计入** 52 §2 的「6 条」；其唯一残余（`tests/e2e/**` 扇出）本轮已由本角色复核完成（见头部 2026-09-20 行）。④ **判定影响**：上述计数增减**不改变** 52 §2 行 4（`不可发布`）的成立性（D2 的成立与否不随条数变化）。⑤ **证据路径**（绝对路径，留存不删）：`D:\AI test\AI-Agents-workflow-Engineer-Rules\tests\e2e\_tmp-obs12-fixture-redis-20260920-01`（含事前公示、覆盖值两轮全量原始日志、对照实验日志、运行后探针实测、事后复位实测）；证据强度 = **旁证**（非版本控制级：dotnet test 原始输出 + redis-cli 探针输出 + 复位实测记录）。⑥ **（2026-09-20 Wave D 更新）**：`51:OBS-12` 已由本角色独立复现 → `已闭环`，末列重标 **非阻断** → ①②的「是否构成第 7 条阻断」口径问题**随闭环消解**（本条不再落在待计阻断项集合内）；③ 的 `60:OBS-32` 与 52 侧计数口径问题继续驻留主对话裁决。
> **（2026-09-20 终批前半段同步）**：本批回写后本表（`51:` 命名空间）**实测分布 = 12 条 = 阻断 1（`51:OBS-08`）/ 非阻断 7 / 已接受（用户决断）4**（改前 = 阻断 2 / 非阻断 6 / 已接受（用户决断）4）——`51:OBS-05` 闭环重标「非阻断」（依据见该行状态回写块与头部本批登记行）。同批 `docs/52-qa-report.md` 分布 = **阻断 1（`52:OBS-09`）/ 非阻断 7 / 已接受（用户决断）4**（12 条；`52:OBS-05` 与 `52:OBS-12` 均闭环重标「非阻断」）。**两侧现行阻断事实 = 1 条**（Playwright `outputDir` 每次运行前自动清空；`51:OBS-08` 与 `52:OBS-09` 为跨侧同一事实、**未闭环**）。`docs/52-qa-report.md` §2 结论行与判定行受本轮调度约束保护、未改（是否重核归终批主对话）。本批取证目录（绝对路径，留存不删）：`D:\AI test\AI-Agents-workflow-Engineer-Rules\_tmp-close-final-half-20260920-01`。**本注上文（含各批登记行与本节此前段落）的分布陈述均为其各自批次的历史快照，按 13.4 不回改；现行分布以本段为准。**
> **（2026-09-20 终批后半段同步）**：本批回写后本表（`51:` 命名空间）**实测分布 = 13 条 = 阻断 0 / 非阻断 9 / 已接受（用户决断）4**（改前 = 阻断 1（`51:OBS-08`）/ 非阻断 7 / 已接受（用户决断）4）—— `51:OBS-08` 闭环重标「非阻断」（依据见该行回写块：闭环程序已落载体 + 两轮独立目录实跑 + 跨轮读回直接证明）；新增 `51:OBS-15`（非阻断）。同批 `docs/52-qa-report.md` 分布 = **12 条 = 阻断 0 / 非阻断 8 / 已接受（用户决断）4**（`52:OBS-09` 同事实闭环重标「非阻断」）。**两侧现行阻断事实 = 0 条**（`51:OBS-08` ≡ `52:OBS-09` 为跨侧同一事实，本轮闭环）。本批取证目录（绝对路径，留存不删）：`D:\AI test\AI-Agents-workflow-Engineer-Rules\tests\e2e\_tmp-obs09-persist-20260920-01`。**本注上文（含各批登记行与本节此前段落）的分布陈述均为其各自批次的历史快照，按 13.4 不回改；现行分布以本段为准。**

---

## 4. 未执行用例及原因

> **2026-09-17 收敛（回归轮，最终态）**：首轮 13 条未执行项**已全部补做完毕，本清单为空**。收敛路径：TC-23 / TC-25 / TC-38b / TC-48c / TC-76 / TC-77 / TC-85（R4，见 `docs/52-qa-report.md` §1.6.3；TC-76 第 4 子项的口径冲突 QX-E-07 经用户裁决后复跑通过，见 §1.6.4）→ TC-78 / TC-79 / TC-79b / TC-84（R5，**4/4 PASS**，见 §1.6.5）→ **TC-56b / TC-56c（R6，破坏性批，2026-09-17 17:08–17:18，2/2 PASS，见 §1.6.6）**。**当前未执行：0 条（98/98 已执行）**。

| TC | 层级 | 状态 |
| --- | --- | --- |
| ~~TC-56b~~ | INT | **已执行（R6，2026-09-17 17:08–17:09，PASS）**：以「另一连接对 `User` 行持 X 锁」注入事务中途失败，失败点经在飞期 `sys.innodb_lock_waits` 证实为第 ⑥ 步 `INSERT INTO WinningRecord`（等待方 `trx_rows_modified=4`，即写入之后才阻塞）；**回滚一致性 5/5 通过**；执行中暴露 **BUG-05**（见 §2）。证据 `tests/e2e/logs/tc56b-real-20260917-170833.log` |
| ~~TC-56c~~ | INT | **已执行（R6，2026-09-17 17:17–17:18，PASS）**：停 `luckydraw-redis` 后重放同一幂等键结果全等、不重复扣次，新键抽奖主链路可用，次数 / 记录查询与库内一致，并取得设计内降级 warning 正向证据；收尾已恢复 Redis 并验证注册 + 抽奖正常。证据 `tests/e2e/logs/tc56c-run2-20260917-171700.log` |

---

## 5. 已执行但结论有偏差的用例（非缺陷）

| TC | 执行结论 | 偏差说明 |
| --- | --- | --- |
| TC-53 | **未达断言口径**（HTTP 200 / `code=0` 重放） | 见 OBS-03：409 分支在 D-03 契约下不可达；核心不变量（仅扣 1 次、无第二次副作用、流水行 1 条）实测满足。**属契约可测性缺口，不计缺陷**，已登记待确认清单 |
| TC-56 | 通过（**缩放执行**） | 契约口径为 50 并发；受每日 3 次 / 用户限制，缩放为「库存 3 / 6 用户并发」。核心不变量（库存 ≥ 0、中奖记录数 = 实际扣减库存数）实测满足 |
| TC-57 / TC-58 / TC-59 | 通过（**夹具等价**） | 见 OBS-06：以「昨日配额行 `UsedCount = 3`」的夹具等价验证跨日重置；逐秒边界未执行 |
| TC-13 | **通过（2026-09-17 复跑已补全）** | 前 5 次 `1102`、第 6 次 `1103`（HTTP 200）、锁定期内正确密码亦被拒 —— 均实测通过。**原被登录限流阻塞的分支已在复跑中验证**：不存在用户名连续错密同样得到 `1102 ×5 → 1103`，即**锁定计数按用户名而非账号存在性**，不构成用户名枚举信道（判据另见 `docs/error-codes.md` §「使用边界」`1103` 条）。证据：`tests/e2e/logs/api-round2-20260917-145013.log` 中的 `[PASS] TC-13b ::` 记录（同文件末尾 JSON 明细 `"tc": "TC-13b"`）；该标签**非 50 定义的用例，属执行侧补验标签**，映射见头部「执行侧补验标签勘误」行 |
| TC-22 | 通过（**口径说明**） | 见 OBS-04：实测跨页面实例合计 2 次 refresh，符合「每实例一次性闸门」的契约语义 |
| TC-81 | 通过（**可检索面受限**） | 见 OBS-05：应用日志未落盘，改以 `AuditLog` / `DrawRequest` / `User.PasswordHash` 交叉验证。**（2026-09-20 终批后半段追记）**CHG-21 后应用日志已落盘为文件，判据面已升级为「**落盘日志正文扫描 + 三面 DB**」（含空转防护与扫描器自证；脚本改造本批落盘、未在就绪环境复跑，见 `docs/52-qa-report.md` §1.6.8 与 `51:OBS-15`）；本行历史结论按 13.4 不回改。 |
| TC-60 | **用例缺陷（已修正，复跑通过）** | 定位符作用域错误：`page-records--default` 挂在 `<TableBody>`（仅表体，`WinningRecordsView.vue:199-202`），表头 `<TableHeader>` 是其上方兄弟节点（`:120-127`），原断言取表体文本查列头文案，**永远不可能通过**。已改为断言 `table thead` 含「奖品名称」「中奖时间」（保留 50 的原意）。**修正后复跑：1 passed**，实测表头文本 `奖品名称 \| 中奖时间`、仅本人 2 条、时间倒序（14:41 → 14:40）。产品侧 AC-15 已满足。**非产品缺陷**，不计入 BUG |

---

## 6. 定级异议登记

（无。engineer 若对 BUG-01 / BUG-02 / BUG-03 的定级有异议，可在本表追加「定级异议」并交由主对话裁决。）

---

## 待确认清单（test-executor）

| 编号 | 缺失信息 | 采用的默认假设 | 若假设不成立的影响 |
| --- | --- | --- | --- |
| QX-E-01（**已裁决（用户决断），2026-09-20**） | 401 响应体形态（`null` / 空体，未经 `ApiResult` 包装）是否属契约要求 | **已裁决（用户决断，2026-09-20；经主对话转达）**：维持**契约空白**判定、不立 BUG；**记入 `docs/52-qa-report.md` §2 残留清单并附补丁计划** = 下一轮把 401 响应体形态**写进契约**（`docs/30-architecture.md` 通用错误响应体形态一次写死），落地后本项闭环 | 若产品要求 401 也返回 `ApiResult` 结构，则为一般缺陷，需 engineer 补全局处理 —— 该分支即补丁计划的对象；契约写死后本待确认项消解 |
| QX-E-02（**已消解，2026-09-20**） | 抽奖接口「同键异体」是否应返回 409（`docs/50-testcases.md` TC-53 的期望结果），抑或 D-03「请求体恒为 `{}`」使其不可达 | **已消解**：按用户 2026-09-20 裁决的**事实级去重**口径，本项与 `51:OBS-03` 为**同一事实** —— 该事实已按 D2 第 2 项改判为 `已接受（用户决断）`（见 §3 `51:OBS-03` 行）→ **不再作为 D2 第 1 项的「未消解」项重复计入** | 无（已消解）；若产品日后要求 draw 也校验请求体哈希，按 `51:OBS-03` 行的补丁计划一并重开 |
| QX-E-03 | 订单 / 战斗类…（无）—— 本行留空占位，不涉及 | — | — |
| QX-E-04 | TC-76 / TC-77 的**部署类验收**是否授权 test-executor 变更运行配置（`dotnet user-secrets`）并重启服务 | **已按不触碰既有环境的方式执行完毕（2026-09-17）**：另起实例 :5191 / :5192，配置**仅经环境变量注入**，未改 `src/` 与任何 `appsettings*.json` / `launchSettings.json`，未触碰主对话的 :5180 / :5173 与开发库 → **无需再授权** | 仅当要求「以 `dotnet user-secrets` 方式重做」时需另行授权 |
| QX-E-05 | TC-78 / TC-79 统计抽样的**执行方式**（是否授权临时提高 `Draw:DailyLimit` 以取得 1 万 / 1 千次样本） | **已执行（2026-09-17 16:42–16:48，R5）→ 无需再授权**：另起 Testing 实例 :5199，`Draw__DailyLimit=30000` / `RateLimit__DrawPerMinute=100000` **仅经环境变量注入**，未改 `src/`、未改任何 `appsettings*.json` / `launchSettings.json`，未触碰主对话实例（:5180 / :5198）与开发库 `luckydraw_dev`；库存以数据夹具钉高并在运行后复位 FR-03 默认值 | 无（已执行完毕）；结果见 `docs/52-qa-report.md` §1.6.5 |
| QX-E-07（**已裁决并闭环**） | 生产构建产物是否允许存在「空的确定性配置节」：D-06 散文（`docs/30-architecture.md:502`「只含 `Enabled=false`」）与 TC-76 原文（`docs/50-testcases.md` **v2** `:264`「不含该节」；**v3** 该行移至 `:268`）/ RSK-04（`30-architecture.md:966`「不含该节」）口径互斥；实测该节存在（`Enabled:false` + `ForcedResults:[]`） | **已裁决（用户决断，2026-09-17）：采用解释 1 —— 判 TC-76 原用词过严，改用例**：test-designer 出 50 v3（第三条款改为断言取值，齐 D-06 配置键表 `:495-500`）→ 用例断言同步修订并复跑 **3 passed（TC-76 4/4 全绿）**；**未立 BUG**、无工程侧改动（功能面：非空覆盖项在生产一律 fail-fast，无泄漏路径） | 无（已闭环）；`30-architecture.md` 散文 `:502` / RSK-04 `:966` 的宽泛措辞是否修订属架构文档自身处置范围，不阻塞本台账结论 |
